# Model Input

- task_id: 24174-customer-latest-info-add-send-date
- stage: m1-requirement-card
- round: round-1
- task: compare-candidates

---

## Instruction

# M1 Requirement Card Candidate Comparator

你现在是 Requirement Card 比较 Agent。

你的任务：
比较 3 张 Requirement Card Candidate，并生成 07-comparison.md。

当前阶段：
只允许比较 Requirement Card Candidate。
不要生成新的 Requirement Card。
不要进入 Engineering Requirement Card。
不要写代码、SQL、patch、diff、实现方案。

比较对象：
- Candidate A: Codex
- Candidate B: Claude
- Candidate C: DeepSeek

比较维度：

1. 是否保留领导原话
2. 是否准确识别主目标
3. 是否区分 已确认 / 推测 / 未知
4. 是否正确处理 表形式レポート / 集計レポート 的范围差异
5. 是否正确保留“最新”口径风险
6. 是否没有进入实现方案
7. Acceptance Criteria 是否可手动验证
8. Unknowns 是否完整
9. 风险与歧义是否充分
10. 是否适合作为后续 frozen requirement card 的基础

特别注意：
- 如果某张 Candidate 把 Unknown 写成了已确认事实，必须指出。
- 如果某张 Candidate 把“最新 = Cdr.start_date 最大值”直接当成确定事实，必须指出。
- 如果某张 Candidate 忽略了集計レポート范围风险，必须指出。
- 如果某张 Candidate 忽略了 CSV / API / 表形式 CSV / 集計 CSV 范围未知，必须指出。
- 如果某张 Candidate 混入实现方案，必须指出。

输出格式：

# Round 1 Candidate Comparison

## 1. 总体结论

## 2. Candidate A: Codex 评价

### 优点
### 问题
### 可吸收内容

## 3. Candidate B: Claude 评价

### 优点
### 问题
### 可吸收内容

## 4. Candidate C: DeepSeek 评价

### 优点
### 问题
### 可吸收内容

## 5. 横向对比表

| 维度 | Codex | Claude | DeepSeek |
|---|---|---|---|

## 6. 关键风险保留情况

## 7. 推荐主卡

## 8. 合并建议

只输出比较报告正文。

---

## Candidate A: Codex

# Requirement Card Candidate

## 1. 原始需求保真

领导原话（保真）：

```text
"【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。"

ベルシステム２４
```

用户补充原话 / 线索（保真）：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

## 2. 一句话需求摘要

希望在 `顧客情報レポート` 的 `顧客最新情報` 里支持展示一个新增项目 `発着信日時（最新）`，用于显示该客户的最新发着信时间；但“最新”的判定口径、是否同时覆盖 `集計レポート` 等仍未确认。

## 3. 用户真实目标

- 已确认：减少用户必须去 `顧客対応履歴` 全量查看并手动抽取最新发信日付的操作。
- 已确认：让报表侧可直接看到与客户相关的“最新发着信时间”。
- 推测：希望报表配置页可勾选该项目，并在输出结果中直接展示。
- 未知：用户真实目标是否只针对 `表形式レポート`，还是也要求 `集計レポート` 一并支持。

## 4. 业务对象与操作路径

- 业务对象
  - 已确认：`顧客情報レポート`
  - 已确认：其中的 `顧客最新情報`
  - 已确认：新增项目名为 `発着信日時（最新）`
  - 推测：数据参考 `cdr` / `発着信履歴`
- 操作路径
  - 已确认：目标页面为 `admin/customer_contact_history_reports/add`
  - 已确认：目标位置为 `項目（*必須）`
  - 已确认：该区域当前包含 `出力 / 項目名 / 表示名 / 順番` 相关配置
  - 未知：完整菜单进入路径未确认
  - 未知：是否同一路径同时控制 `表形式レポート` 与 `集計レポート`

## 5. 当前现状

- 已确认：`admin/customer_contact_history_reports/add` 的 `項目（*必須）` 为配置型字段列表，不是页面写死的单个业务字段。
- 已确认：当前 `顧客最新情報` 对应字段列表中没有 `発着信日時（最新）` / `Cdr.start_date`。
- 已确认：当前报表输出依赖已保存的字段配置做展示映射。
- 已确认：`顧客最新情報` 现有代码链路里的“最新”口径，实际是“每客户取最新 `customer_notes.id` 后再关联 cdr”，并不是直接取 `cdr.start_date` 最大值。
- 已确认：`/admin/cdr/index` 页面中的 `発着信時間` 来源是 `cdr.start_date`。
- 已确认：`項目（*必須）` 只直接出现在表形式配置区域；`集計レポート` 使用另一套 X/Y/V 配置方式。
- 推测：如果本次仅追加到当前 `項目（*必須）` 列表，直接影响的是 `表形式レポート`。
- 未知：无 `発着信履歴` 数据时，当前业务预期应该显示空白、`-`、`（空白）` 还是其他值。
- 未知：CSV、API、其他导出是否属于本次验收范围。

## 6. 期望结果

- 已确认：在 `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 中出现 `発着信日時（最新）`。
- 已确认：该项目可被选择，并在报表输出中展示。
- 已确认：该项目应出现在 `項目名` / `表示名` 相关配置中。
- 已确认：`順番` 需要“放在最后一位”。
- 推测：数据语义应为“该客户最新一条发着信履历对应的时间”。
- 未知：“最新”到底按 `cdr.start_date` 最大值，还是沿用现有 `顧客最新情報` 的 `customer_notes.id` 最大值逻辑。
- 未知：`集計レポート` 是否也必须支持同一语义的项目。
- 未知：CSV / API / 表形式 CSV / 集計 CSV 是否必须同步体现。

## 7. 明确范围 In Scope

- 已确认：围绕 `顧客情報レポート` 中 `顧客最新情報` 的新增展示需求进行定义。
- 已确认：围绕 `admin/customer_contact_history_reports/add` 页面 `項目（*必須）` 中新增 `発着信日時（最新）` 这一候选需求进行整理。
- 已确认：围绕“勾选后，报表中可展示该字段”这一业务结果进行定义。
- 已确认：围绕“顺番放在最后一位”这一展示要求进行记录。
- 包含但待确认：该需求是否扩展到 `集計レポート`、CSV、API 等派生输出。

## 8. 明确不做 Out of Scope

- 不进入实现方案。
- 不进入 Engineering Requirement Card。
- 不指定必须修改的代码文件。
- 不写代码、SQL、patch、diff。
- 不替业务决定“最新”的技术口径。
- 不默认把代码调查中的“可能受影响输出”升级为“本次必须一起改”。
- 不把 `表形式レポート` / `集計レポート` / CSV / API 的范围未确认项直接定版。
- 不处理与本需求无直接关系的权限、菜单路径、批处理、邮件配信等扩展议题，除非后续明确纳入。

## 9. 需要确认 Unknowns

- 这次是否只要求 `表形式レポート` 支持 `発着信日時（最新）`。
- `集計レポート` 是否也必须支持 `発着信日時（最新）`。
- `発着信日時（最新）` 的“最新”口径，到底是 `Cdr.start_date` 最大值，还是现有 `顧客最新情報` 的 `customer_notes.id` 最大值逻辑。
- 无 `発着信履歴` 数据时应该显示什么。
- `順番` 的“最后一位”是指系统字段最后，还是包含自定义字段后的全列表最后。
- CSV 是否属于本次验收范围。
- `api_find_report` 是否需要同步影响。
- 表形式 CSV / 集計 CSV 是否属于本次范围。
- 用户补充里出现了 `* 集計レポート`，这是否代表明确业务要求，还是仅为调查线索。

## 10. 验收标准 Acceptance Criteria

- AC-01：访问 `admin/customer_contact_history_reports/add`，在 `顧客最新情報` 对应的 `項目（*必須）` 中，可手动看到 `発着信日時（最新）`。
- AC-02：`発着信日時（最新）` 在配置界面中可被手动勾选，并可保存到报表配置。
- AC-03：保存后查看对应报表时，可手动确认报表中出现 `発着信日時（最新）` 对应展示。
- AC-04：当同一客户存在多条 `発着信履歴` 时，报表展示值应与“已确认的最新口径”一致。
- AC-05：当同一客户不存在 `発着信履歴` 时，报表展示值应与“已确认的无数据显示规则”一致。
- AC-06：既有项目的显示名、顺番、勾选行为在手动回归时不应异常变化。
- AC-07：若后续确认本次范围包含 `集計レポート`，则需手动验证 `集計レポート` 也可按确认后的规则使用或展示该项。
- AC-08：若后续确认本次范围包含 CSV / API，则需分别手动验证其输出结果与页面规则一致。

## 11. 风险与歧义

- 已确认风险：当前 `顧客最新情報` 代码链路中的“最新”并不是按 `cdr.start_date` 取值；如果业务理解的“最新”是发着信时间最新，则现状口径与需求语义可能冲突。
- 已确认风险：`/admin/cdr/index` 的 `発着信時間` 虽来自 `cdr.start_date`，但页面默认排序更接近 `id DESC`，不能直接等同为严格 `start_date DESC` 口径。
- 已确认风险：`表形式レポート` 与 `集計レポート` 当前配置方式不同，不能自然推定“一个新增项会自动同时适用于两者”。
- 已确认风险：CSV、API 已存在输出链路，若需求范围包含它们，会扩大验收面。
- 已确认风险：无数据时现有不同路径存在空串 / `N/A` / `（空白）` 等不同表现，若不先定业务预期，验收容易反复。
- 歧义：领导原话写的是 `発信日付`，用户补充写的是 `発着信日時（最新）`，两者在“仅发信”还是“发着信”、以及“日付”还是“日時”上存在表述差异。
- 歧义：`順番` 的“最后一位”未说明参照的是哪一组字段边界。

## 12. 给后续 Engineering Requirement Card 的输入提醒

- 必须原样带入领导原话与用户补充原话，不能替换成实现口径。
- 必须先补齐 Unknowns，尤其是：
  - `表形式レポート` 是否为唯一范围
  - `集計レポート` 是否纳入
  - “最新”口径到底按 `cdr.start_date` 还是按现有 `customer_notes` 逻辑
  - 无数据展示规则
  - `順番` 的边界定义
  - CSV / `api_find_report` / 表形式 CSV / 集計 CSV 是否纳入
- 必须把“代码已确认的现状”与“业务要什么”分开写，不能因为现状如此就默认业务要求如此。
- 不能把“可能相关输出链路”写成“必须一起改的范围”。
- 后续工程卡若引用代码调查结论，只能作为需求澄清依据，不能直接跳成实现指令。

---

## Candidate B: Claude

# Requirement Card Candidate

## 1. 原始需求保真

领导原话：

```text
"【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。"

ベルシステム２４
```

用户补充原话 / 线索：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

---

## 2. 一句话需求摘要

在 `admin/customer_contact_history_reports/add` 页面「顧客最新情報」对应配置下的 `項目（*必須）` 中追加一个可选项目 `発着信日時（最新）`，使生成的 report 能直接显示该客户最新一条発着信履歴的発着信時間，免去现在必须从「顧客対応履歴」全履歴中人工筛选的步骤。

---

## 3. 用户真实目标

- 已确认：让使用「顧客最新情報」レポート的用户能够直接看到该客户的最新発信日付，不再需要回到「顧客対応履歴」逐条翻看后再人工提取。
- 已确认：消除当前操作链路中“看顧客最新情報 → 切换到顧客対応履歴 → 全履歴中找最新発信日付”这一段重复劳动。

---

## 4. 业务对象与操作路径

- 目标页面：`admin/customer_contact_history_reports/add`
- 目标区块：`項目（*必須）`
- 目标列：
  - `出力 項目名`
  - `表示名`
  - `順番`
- 追加项目名（出力 項目名 / 表示名）：`発着信日時（最新）`
- 順番位置：放在最后一位（“最后一位”的精确语义见 Unknowns）
- 参考页面：`/admin/cdr/index`
- 参考字段：`発着信時間`（代码调查显示对应 `cdr.start_date`）
- 完整菜单路径：未知

---

## 5. 当前现状

- 已确认：`admin/customer_contact_history_reports/add` 在 `target=2（顧客最新情報）` 的系统字段列表里没有 `Cdr.start_date`，仅 `target=1（顧客対応履歴）` 有。
- 已确认：`target=2（顧客最新情報）` 的“最新”当前是以 `customer_notes.max(id)` 关联最新 note，再 left join 到 cdr，并非按 `cdr.start_date` 取最新。
- 已确认：用户当前需要从「顧客対応履歴」全履歴中自行确认最新発信日付。
- 已确认：`/admin/cdr/index` 中「発着信時間」对应 `cdr.start_date`；页面默认按 `id DESC` 排序，不严格等同于 `start_date DESC`。
- 已确认：`項目（*必須）` 区块仅在表形式（type=1）显示；集計（type=2）使用 X/Y/V 下拉。
- 已确认：`target=2` 的集計 SQL 当前未 join `cdr`。
- 已确认：CSV 下载存在两类（表形式 / 集計）；未发现 Excel / PDF / batch / cron / 邮件配信派生输出；存在 `api_find_report` 对外接口。
- 未知：无発着信履歴数据时既存类似字段的统一展示规则。

---

## 6. 期望结果

- 已确认：在 `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 中出现 `発着信日時（最新）` 一项。
- 已确认：`発着信日時（最新）` 可被用户勾选。
- 已确认：在 `出力 項目名` 列与 `表示名` 列显示名均为 `発着信日時（最新）`。
- 已确认：`順番` 放在最后一位。
- 已确认：勾选后生成 / 查看 report 时，能展示该客户最新一条発着信履歴对应的発着信時間。
- 推测：多条発着信履歴存在时，取“最新一条”记录的発着信時間（“最新”的具体口径见 Unknowns）。
- 未知：无発着信履歴数据时的显示规则（空 / `-` / `N/A` / `(空白)` 等）。

---

## 7. 明确范围 In Scope

- 已确认：`admin/customer_contact_history_reports/add` 页面 `項目（*必須）` 中追加 `発着信日時（最新）` 项目（出力 項目名 + 表示名 + 順番=最后一位）。
- 已确认：勾选 `発着信日時（最新）` 后，report（至少表形式 / target=2 路径）画面能展示该项目。
- 已确认：与 `admin/customer_contact_history_reports/add` 配对的编辑页 / 查看页（`admin_edit` / `admin_view`）对该新增项目的展示与勾选保持一致。
- 已确认：日语文案 `発着信日時（最新）`。

---

## 8. 明确不做 Out of Scope

- 已确认：搜索条件不在范围内。
- 已确认：权限控制不在范围内。
- 已确认：Excel / PDF 输出不在范围内（代码调查未发现此实现）。
- 已确认：batch / cron 不在范围内（代码调查未发现关联调用链）。
- 已确认：邮件配信内容不在范围内（代码调查未发现关联）。
- 推测：`/admin/cdr/index` 自身不做修改（仅作参考页面）。
- 推测：`顧客対応履歴`（target=1）侧的既存 `Cdr.start_date` 行为不做修改。

> 注意：CSV、集計レポート、`api_find_report` 是否属于本次范围未确认，置于 Unknowns，不在此处提前判定。

---

## 9. 需要确认 Unknowns

1. 本次是否只要求「表形式レポート」（type=1）支持 `発着信日時（最新）`。
2. 「集計レポート」（type=2）是否也必须支持 `発着信日時（最新）`（涉及 X/Y/V 下拉及 target=2 集計 SQL 是否需要 join cdr）。
3. `発着信日時（最新）` 的“最新”判定口径：
   - 候选 A：`cdr.start_date` 最大值
   - 候选 B：沿用现有「顧客最新情報」链路口径，即 `customer_notes.max(id)` 关联到的 cdr
   - 两者可能不一致，需领导侧明确。
4. 客户无発着信履歴数据时，`発着信日時（最新）` 应显示为：空字符串 / `-` / `N/A` / `(空白)` / 其他。
5. `順番` “最后一位” 的精确语义：
   - 候选 A：仅指系统字段数组的最后
   - 候选 B：包含自定义字段（`CustomerContactExt.field*`）后的全列表最后
6. CSV 下载（表形式 CSV / 集計 CSV）是否属于本次验收范围。
7. `api_find_report` 对外行为是否需要同步影响（尤其 target=2 时）。
8. 完整菜单操作路径（用户如何抵达 `admin/customer_contact_history_reports/add`）未确认；不影响需求本身，但影响验收手动操作描述。

---

## 10. 验收标准 Acceptance Criteria

- AC-001：访问 `admin/customer_contact_history_reports/add`，在 target=「顧客最新情報」配置下，`項目（*必須）` 中可见 `発着信日時（最新）` 一项。
- AC-002：`発着信日時（最新）` 在 `出力 項目名` 列与 `表示名` 列均显示为 `発着信日時（最新）`。
- AC-003：`発着信日時（最新)` 的 `順番` 出现在 `項目（*必須）` 列表的最后一位（“最后一位”精确语义以 Unknown 5 的最终决定为准）。
- AC-004：`発着信日時（最新）` 可被用户勾选并成功保存。
- AC-005：勾选 `発着信日時（最新）` 后生成 / 查看 report，画面中能看到 `発着信日時（最新）` 列。
- AC-006：当客户存在 1 条以上発着信履歴时，`発着信日時（最新）` 列展示的值与“最新”口径决定后的对应記録の発着信時間一致(口径以 Unknown 3 的最终决定为准)。
- AC-007：当客户无発着信履歴数据时,`発着信日時（最新）` 列的显示与 Unknown 4 决定后的规则一致。
- AC-Regression-001：未勾选 `発着信日時（最新）` 时,既存 report 的其他项目显示不受影响。
- AC-Regression-002：既存 `項目（*必須）` 中原有项目的显示名、順番、勾选行为不受影响。
- AC-Regression-003：`target=1（顧客対応履歴）` 侧的既存 `Cdr.start_date` 项目行为不受影响。
- AC-Regression-004：`/admin/cdr/index` 行为不受影响。

> 集計レポート / CSV / `api_find_report` 相关 AC 待 Unknowns 1、2、6、7 确认后追加。

---

## 11. 风险与歧义

- 风险 1：“最新”口径分歧。现有「顧客最新情報」链路用 `customer_notes.max(id)`，业务表达可能期望 `cdr.start_date DESC`。两者在数据上可能不一致，若实现侧未明确口径则验收易出歧义。
- 风险 2：集計レポート（type=2）一旦也要支持，会触及 target=2 集計 SQL 当前未 join cdr 的事实，影响范围远大于仅在表形式追加一项字段。
- 风险 3：「順番最后一位」语义模糊。系统字段最后 vs 包含自定义字段后的全列表最后，会带来视觉顺序差异。
- 风险 4：无 cdr / 无 note 时既存路径已有 空串 / `(空白)` / `N/A` 多种表现，未统一可能导致用户感知不一致。
- 风险 5：CSV / `api_find_report` 等派生输出是否同步存在“做了一半”的隐患——若仅画面追加，下载或对外接口可能与画面不一致。
- 风险 6：`/admin/cdr/index` 默认按 `id DESC` 排序而非 `start_date DESC`，参考页面的“最新”视觉并非严格 `start_date` 最新，作为口径依据存在隐性偏差。
- 风险 7：`privilege.conf` 中存在未实现 action 的权限项，历史遗留配置可能干扰影响范围判断，但与本次需求无直接业务关系。
- 歧义 1：领导原话用「発信日付」，用户补充用「発着信日時（最新）」。前者偏“発信”（去电），后者覆盖“発着信”（去电+来电）。两者在 cdr 数据范围上可能不同。

---

## 12. 给后续 Engineering Requirement Card 的输入提醒

- 必须先收敛以下 Unknowns 再进入 Engineering 阶段：1（type=1 范围）、2（type=2 是否支持）、3（“最新”口径）、4（无数据显示规则）、5（順番最后一位精确语义）、6（CSV 范围）、7（`api_find_report` 范围）。
- 必须明确歧义 1：本次是「発信」还是「発着信」，对应 cdr 数据筛选条件不同。
- 代码调查报告中列出的“相关文件清单 / 最小修改候选范围”仅作为候选信息，不得在本卡阶段升级为“必须修改文件”。Engineering Requirement Card 应基于收敛后的 Unknowns 再行决定实际修改面。
- 业务口径与现有「顧客最新情報」链路口径不一致时，需领导侧拍板，不得由实现侧自行决定。
- 验收标准中已置为“待确认”的项目（集計 / CSV / API）需在 Unknowns 收敛后补全 AC。
- 不在本卡产出 SQL / 实现方案 / patch / diff。

---

## Candidate C: DeepSeek

# Requirement Card Candidate

## 1. 原始需求保真

```text
"【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。"

ベルシステム２４
```

用户补充原话 / 线索：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

## 2. 一句话需求摘要

在 `admin/customer_contact_history_reports/add` 页面中，为「顧客最新情報」目标报表，在 `項目（*必須）` 区域内追加一个可选字段 `発着信日時（最新）`，用于显示该客户最新一条発着信履歴的 `発着信時間`。

## 3. 用户真实目标

用户（ベルシステム２４）希望不再需要手动从「顧客対応履歴」的全履历中自行确认最新发信日期，而是能够：

- 在「顧客最新情報」报表配置页面中，直接勾选 `発着信日時（最新）` 这个字段
- 报表输出时，直接看到针对每个客户自动取出的最新発着信时间
- 从而减少手工核对步骤，提升业务效率

## 4. 业务对象与操作路径

**业务对象**

- 报表类型：`表形式レポート`（type=1），以及可能涉及的 `集計レポート`（type=2）
- 报表目标：`顧客最新情報`（target=2）
- 追加字段：`発着信日時（最新）`
- 数据来源：`cdr` 表（発着信履歴），参考字段 `発着信時間`（cdr.start_date）
- 关联实体：CustomerContactHistoryReport、CustomerContactHistoryReportField、cdr、customer_notes、customer_contacts

**操作路径**

1. 系统管理员 / 有权限用户访问报表配置页面：
   - 已确认目标URL：`admin/customer_contact_history_reports/add`
   - 完整菜单路径未确认
2. 在新建 / 编辑报表页面，选择 `対象` 为「顧客最新情報」时，`項目（*必須）` 区域出现字段列表
3. 用户勾选 `発着信日時（最新）`，并可设置其「表示名」「順番」
4. 保存报表配置
5. 查看或导出报表时，`発着信日時（最新）` 列展示每个客户的最新発着信时间

## 5. 当前现状

根据代码调查报告已确认：

| 维度 | 现状 |
|------|------|
| `項目（*必須）` 字段源 | `target=2`（顧客最新情報）的系统字段列表中不包含任何 Cdr 相关字段；只有 `target=1`（顧客対応履歴）才包含 `Cdr.start_date`。 |
| 报表输出逻辑 | `target=2` 的报表查询，每个客户是通过 `customer_notes` 最大 `id` 关联最新 note，再 left join cdr 表。并非按 `Cdr.start_date` 降序取最新発着信履歴。 |
| 页面显示 | 配置页面的「項目（*必須）」四列（出力、項目名、表示名、順番）由 `tableSystemFields` 数组渲染，`順番` 默认按数组追加顺序递增。 |
| 无数据展示 | 报表无值时部分分支输出空字符串，部分输出 `N/A` 或 `global.blank`，尚无统一约定。 |
| CSV下载 | 表形式与集計均存在 CSV 下载功能，新增字段会影响对应 CSV 输出列。 |
| 集計レポート | 集計报表配置界面使用 X/Y/V 下拉，不在同一「項目（*必須）」列表内；且 `target=2` 集計 SQL 未 join cdr。 |
| API | 存在 `api_find_report` 接口基于已配置字段输出 CSV。 |
| /admin/cdr/index | 「発着信時間」显示为 `cdr.start_date` 字段，默认排序为 `id DESC`（并非严格按 `start_date DESC`）。 |
| 权限 | privilege.conf 中存在一个未实现对应 action 的 `admin_report_detail_download` 权限项。 |

## 6. 期望结果

1. 当用户在 `admin/customer_contact_history_reports/add`（及编辑页）选择 `対象=顧客最新情報` 并选择报表类型为 `表形式` 时，`項目（*必須）` 中出现 `発着信日時（最新）`，位于「出力 項目名」列和「表示名」列，并且順番默认安排在所有字段的最后一位。
2. 用户可以勾选该字段并保存。
3. 在已保存报表的查看页面（`admin_report_detail_view`）及对应的 DataTables 列表、表形式 CSV 下载中，能展示该字段值。
4. 对于每个客户，该字段显示的值取自该客户発着信履歴中最新的 `発着信時間`（口径需确认，现暂按需求提到的“cdr参照”理解为基于 Cdr 表的最新年月日时）。
5. 已存在的其他字段、显示名、順番、勾选行为不受影响。
6. 未勾选 `発着信日時（最新）` 时，报表输出与现有保持完全一致。

## 7. 明确范围 In Scope

1. **画面追加**  
   - 在 `admin/customer_contact_history_reports/add` 和 `admin/customer_contact_history_reports/edit` 页面，当 `target=2` 且 `type=1`（表形式）时，`項目（*必須）` 的系统字段列表中追加 `発着信日時（最新）`。
2. **表示名与順番**  
   - 表示名（默认）：`発着信日時（最新）`  
   - 順番：追加在现有系统字段及可能的自定义字段之后（最后一位）。
3. **保存与读取**  
   - 用户勾选该字段后，字段信息正常存入 `customer_contact_history_report_fields`。  
   - 报表配置查询 / 编辑时能正确回显勾选状态。
4. **报表输出**  
   - 表形式报表查看页面（DataTables 列表）能显示该列。  
   - 表形式 CSV 下载包含该列，列头使用设定的表示名。
5. **数据取值**  
   - 採用该客户最新的発着信履歴时间（具体口径根据确认后实现，默认期望按 `Cdr.start_date` 降序取第一条）。  
   - 字段名建议为代码方便映射的名称，如 `cdr_latest_start_date`。
6. **回归保护**  
   - 未勾选时报表完全不变。  
   - `target=1` 及其他报表目标不受影响。  
   - 既存报表的原有字段、順番、表示名不变。

## 8. 明确不做 Out of Scope

1. **不涉及 `集計レポート`（type=2）**  
   - 本次不要求集計レポート的 X/Y/V 下拉中追加该字段。  
   - 不修改 `target=2` 集計 SQL 加入 cdr join。  
   - 若后续需要，作为独立需求提出。
2. **不修改 `/admin/cdr/index` 页面**  
   - 该页面仅为业务参考，本次不直接改动其功能或排序逻辑。
3. **不新增 Excel / PDF 导出或邮件配信功能**  
   - 若已有 CSV 之外导出，当前不扩展。
4. **不修改 API 接口的数据结构**  
   - `api_find_report` 暂不保证本次追加字段自动进入其输出（需单独确认后决定）。
5. **不处理无発着信履歴时的特殊显示逻辑设计**  
   - 待 Unknowns 中确认后，由后续工程卡定义。
6. **不修改权限配置**  
   - 不新增或调整 privilege.conf 中未实现的 action 项。

## 9. 需要确认 Unknowns

1. **最新口径定义**  
   「発着信日時（最新）」的“最新”判定口径，是以 `Cdr.start_date` 最大值为准，还是沿用现有「顧客最新情報」中 `customer_notes.id` 最大值的逻辑？  
   领导原话“発着信履歴に一番最新→ cdr参照”偏向于 Cdr，但现有代码实现并不一致，需要最终确认。

2. **是否仅限于「表形式レポート」**  
   用户补充中提及了“表形式レポート”和“集計レポート”，但代码调查显示集計报表字段机制不同。本次要求是否只要求 `表形式レポート` 支持？还是同时要求 `集計レポート` 也能通过某种方式（即使不是同一UI）选择该字段？若只要求表形式，集計明确排除；若需要集計，则影响面显著扩大。

3. **无発着信履歴数据时的显示值**  
   客户没有任何発着信履歴时，该字段应显示什么？  
   - 空（空白）  
   - `-`  
   - `該当なし`  
   - 沿用类似 `customer_notes` 无数据时的表现（当前为 `N/A` 或 `global.blank`）

4. **順番“最后一位”的精确定义**  
   “順番放在最后一位”是指：  
   - 仅在所有系统字段（如 `customer_notes.note_1` 等）之后，但先于可能存在的自定义字段 `CustomerContactExt.field1~`？  
   - 还是在所有字段（含自定义字段）的最后？  
   请确认希望按哪种顺序。

5. **CSV 下载属于验收范围吗？**  
   表形式レポート的 CSV 下载是否需要作为验收的一部分并确保新列出现？目前代码中 CSV 标题使用字段 `name`，数据取 `field_name`，如果新字段按现有模式实现，CSV 会自然跟随。请确认是否需要在验收标准中显式包含 CSV 验证。

6. **`api_find_report` 是否需要同步影响**  
   该 API 当前基于已配置字段输出 CSV。本次是否需要保证当报表 `target=2` 时，新字段同样出现在 API 输出中？如果不作要求，建议后续独立提出。

7. **是否有多语言要求**  
   当前仅有日文表示名要求，但系统支持多语言包。是否考虑其他语言（如中文、英文）的翻译？此次是否仅日文即可？

8. **权限控制**  
   是否需要限制该字段在某种权限下不可见或不可选？目前需求未提及权限，默认不做限制。

9. **报表查看页面中的列宽、排序、筛选**  
   是否需要为新列指定默认列宽、是否可排序、是否可筛选？还是采用系统默认行为即可？

## 10. 验收标准 Acceptance Criteria

* **AC-001**  
  登录系统，访问 `admin/customer_contact_history_reports/add`，选择対象为「顧客最新情報」，报表类型为「表形式」。  
  验证 `項目（*必須）` 区域中出现 `発着信日時（最新）`，位于「出力 項目名」和「表示名」列，且順番在所有可选字段的最后一位。

* **AC-002**  
  勾选 `発着信日時（最新）` 并填写表示名（如需修改），保存报表。  
  验证保存成功，进入编辑页时该字段仍为勾选状态，表示名、順番显示正确。

* **AC-003**  
  在报表查看页面（表形式）中，对于存在発着信履歴的客户，`発着信日時（最新）` 列展示该客户最新的発着信时间（具体时间与手动查询 Cdr 表按业务口径排序的第一条记录相符）。  
  多条履历时展示时间正确为最新一条。

* **AC-004**  
  在表形式レポート的 CSV 下载中，包含 `発着信日時（最新）` 列头（显示名）及对应的数据行。

* **AC-005**  
  在报表查看页面中，取消勾选的报表或创建未勾选 `発着信日時（最新）` 的报表时，报表列表不出现该列，且所有原字段显示仍正常，不受任何影响。

* **AC-006**  
  修改已有旧报表（対象=顧客最新情報、表形式）时，原有勾选字段的表示名、順番、勾选状态全部保持不变，新字段默认为未勾选，位于最后。

* **AC-007**  
  当某客户没有任何発着信履歴数据时，`発着信日時（最新）` 列显示为已确认的显示值（例如空、`-` 等）。此条验证需在 Unknowns 第3项确认后执行。

* **AC-Regression-001**  
  `target=1`（顧客対応履歴）报表的追加/编辑/查看/CSV下载不受任何影响。  
  集計レポート的字段选择、输出不受任何影响。

## 11. 风险与歧义

1. **业务口径偏差风险**  
   现有「顧客最新情報」的“最新”是基于 `customer_notes.id` 最大来关联 note，并非基于 `Cdr.start_date`。如果本次严格按照“cdr参照”取最新発着信時間，则同一客户可能在该报表中出现“最新 note”与“最新発着信”来源不一致的情况（一个取 note 最新，一个取 cdr 最新）。若用户预期是“同一个最新时点的信息”，需要额外协调。

2. **性能风险**  
   查询中增加一个按 `Cdr.start_date DESC` 取最新一条的子查询或窗口函数，在客户和 cdr 数据量较大时可能影响报表生成性能。目前最高效方案可能与现有 `customer_notes.max(id)` 子查询有所不同，需在 Engineering Requirement Card 中设计并评估。

3. **集計レポート遗漏风险**  
   用户补充文本中提到了“集計レポート”，如果最终确认需要支持，当前 In Scope 未包含，将引发返工和额外影响面（需要改动集計 SQL，涉及 group by 和聚合逻辑，可能产生更多歧义）。

4. **順番冲突风险**  
   如果系统字段、自定义字段数量较多，且需求“最后一位”定义不明确，可能导致字段排列不符合用户预期，引发客服投诉或再次调整。

5. **无数据显示不一致风险**  
   代码中不同路径空值处理已不统一（空串、`N/A`、`（空白）` 等）。直接复用某一路径可能与其他字段表现不一致，需要统一策略，否则用户感知混乱。

6. **CSV 输出编码/转义问题**  
   新增日期时间字段在 CSV 输出时可能存在格式转换或特殊字符转义问题，需在开发后加以验证。

7. **API 接口兼容性**  
   若 `api_find_report` 不跟随本次需求变更，外部调用方可能无法获取新字段；反之若变更未通知使用者，可能影响其解析逻辑。需确认影响范围并通知相关方。

8. **只读/安全约束误解风险**  
   未实现的权限项 `admin_report_detail_download` 可能误导认为存在下载权限控制，实际无效。需留意后续清理。

## 12. 给后续 Engineering Requirement Card 的输入提醒

1. **必须优先澄清 Unknowns**  
   特别是“最新口径”（Cdr.start_date 还是 customer_notes.id）、“集計レポート是否纳入范围”、“无数据时的显示值”和“順番的精确位置”。在未确认前，后续 Engineering 不能开始设计实现。

2. **准确定义字段映射标识**  
   新增字段的内部 `field_name`（如 `cdr_latest_start_date`）需要被 mapping logic 识别，并加入表形式 target=2 的查询、DataTables 输出、CSV 输出、保存/读取模型。

3. **查询性能设计**  
   需要给出针对按 `Cdr.start_date` 取最新记录的高效 SQL 方案（如 lateral join、子查询、窗口函数或应用层聚合），并与现有 `customer_notes.max(id)` 逻辑隔离或兼容。

4. **兼容性注意事项**  
   - 保持 `target=1` 和 `target=2` 的字段列表完全独立，改动不影响 target=1。  
   - 仅 type=1 表形式受影响，集計部分代码不得触碰。  
   - 不更改既存字段的默认 ordering 和已保存数据，純粋に追加する。

5. **测试要求**  
   - 覆盖有履历、无履历、多条履历、单条履历的客户数据。  
   - 覆盖新旧报表（含已保存配置）的兼容性。  
   - 覆盖表形式 CSV 下载列序和内容。  
   - 回归测试集計レポート不受影响。

6. **国际化（i18n）**  
   至少提供日文表示名默认值，并建议在语言文件中预留 key，方便日后扩展。

7. **文档与通知**  
   如果确认 `api_find_report` 需要连带变更，需通知集成方；否则在需求中注明不包含。

8. **遗留清理提示**  
   privilege.conf 中的无效权限虽不处理，但可作为 technical debt 记录，后续单独清理。
