# Model Input

- task_id: 99999-m1-auto-smoke-test
- stage: m1-requirement-card
- round: round-2
- task: score-candidates

---

## Instruction

# M1 Requirement Card Candidate Scorer

你现在是 Requirement Card 评分 Agent。

你的任务：
基于 3 张 Requirement Card Candidate 和 07-comparison.md，生成 08-score.json。

当前阶段：
只允许评分 Requirement Card Candidate。
不要生成新的 Requirement Card。
不要进入 Engineering Requirement Card。
不要写代码、SQL、patch、diff、实现方案。

评分对象：
- codex
- claude
- deepseek

评分维度：
每个维度 0-10 分，10 分最好。

维度如下：

1. fidelity_to_original_request
   是否保留领导原话，是否忠实于用户补充，不美化、不改写。

2. requirement_clarity
   需求摘要、目标、业务对象、现状、期望是否清晰。

3. scope_control
   是否控制范围，不把未知项提前放进 In Scope / Out of Scope。

4. unknowns_quality
   Unknowns 是否完整、准确、没有被脑补消除。

5. risk_detection
   是否准确识别“最新”口径、表形式/集計范围、CSV/API 派生影响等风险。

6. acceptance_criteria_quality
   AC 是否可手动验证，是否包含正常场景、空数据、多条数据、回归场景。

7. no_implementation_leakage
   是否没有混入实现方案、SQL、字段命名、patch、文件修改计划。

8. usefulness_for_next_stage
   是否适合作为 frozen Requirement Card 的基础，并能为后续 Engineering Requirement Card 提供清晰输入。

输出必须是严格 JSON。
不要输出 Markdown。
不要输出解释性正文。
不要用代码块包裹 JSON。

输出格式：

{
  "round": "round-1",
  "scores": {
    "codex": {
      "fidelity_to_original_request": 0,
      "requirement_clarity": 0,
      "scope_control": 0,
      "unknowns_quality": 0,
      "risk_detection": 0,
      "acceptance_criteria_quality": 0,
      "no_implementation_leakage": 0,
      "usefulness_for_next_stage": 0,
      "total": 0,
      "summary": ""
    },
    "claude": {
      "fidelity_to_original_request": 0,
      "requirement_clarity": 0,
      "scope_control": 0,
      "unknowns_quality": 0,
      "risk_detection": 0,
      "acceptance_criteria_quality": 0,
      "no_implementation_leakage": 0,
      "usefulness_for_next_stage": 0,
      "total": 0,
      "summary": ""
    },
    "deepseek": {
      "fidelity_to_original_request": 0,
      "requirement_clarity": 0,
      "scope_control": 0,
      "unknowns_quality": 0,
      "risk_detection": 0,
      "acceptance_criteria_quality": 0,
      "no_implementation_leakage": 0,
      "usefulness_for_next_stage": 0,
      "total": 0,
      "summary": ""
    }
  },
  "winner": "",
  "merge_policy": {
    "base_candidate": "",
    "absorb_from_codex": [],
    "absorb_from_claude": [],
    "absorb_from_deepseek": [],
    "discard": []
  }
}

---

## 07-comparison.md

# Round 2 Candidate Comparison

## 1. 总体结论

三张 Candidate 整体水平都很高，均严格保留了领导原话、严格区分了已确认 / 推测 / 未知，没有把核心 Unknown（"最新 = Cdr.start_date 最大值"）写成确定事实，没有进入实现方案、SQL 或 patch。三者都识别出了表形式 / 集計 / CSV / API 的范围未知，并将其作为 Unknown 保留。

差异主要体现在：

- **Claude (B)** 在第 6 节明确把「展示项目」与「最新判定口径」做了显式分离，并在 AC 中专门列出「依赖 Unknown 的待补 AC」段落，结构最严谨，最适合作为 frozen 主卡的基础。
- **Codex (A)** 与 Claude 几乎同等水平，Unknowns 与风险条目最齐全（14 / 13 条），表达稍冗长，但完备性最高。
- **DeepSeek (C)** 整体合格，但在「当前现状」与「风险」段轻度泄露了代码调查的实现细节（`customer_notes.id 最大值`、`/admin/cdr/index 的デフォルト表示順が id DESC`），且 Unknowns（11 条）与风险（10 条）的覆盖面相对偏窄，缺少多语言文案 / 列宽 / 排序 / 筛选等表现层条目。

---

## 2. Candidate A: Codex 评价

### 优点
- 领导原话与补充线索完整保留，未改写。
- Unknowns 14 条、风险 13 条，覆盖面最广，包含「角色级权限可见性」「多语言」「列宽 / 排序 / 筛选」「`/admin/cdr/index` 视觉最新 ≠ 严格最新」等隐性风险。
- 严格区分已确认 / 推测 / 未知，没有把任何 Unknown 转写为事实。
- 「最新」口径明确作为 Unknown #3 保留，未把 `Cdr.start_date 最大值` 当成既定规则。
- 集計レポート、CSV、表形式 CSV、集計 CSV、`api_find_report` 全部作为待确认范围分别列出。
- AC 全部可手动验证，并显式声明依赖 Unknown 的 AC（AC-010 ~ AC-012）以 Unknowns 确认结果为准。
- 第 12 节给后续 Engineering Requirement Card 的提醒最完整，明确禁止把「可能相关文件」升级为「必须修改文件」。

### 问题
- 第 5 节「当前现状」中「当前 `顧客最新情報` 链路中的"最新信息"现有实现口径与最新 note 有关」属于代码调查结论的轻度引入；虽然作为「现状」描述尚可接受，但比 Claude 的表述略具体。
- 整体偏长，部分条目与第 11 节风险存在轻度重复（如 Unknowns #9 / 风险 #4 / 风险 #5 / 风险 #6 之间有交叉）。
- 一句话需求摘要中「但该字段"最新"的判定口径及影响范围仍待确认」与第 6 节有重复表述。

### 可吸收内容
- 第 12 节「测试数据准备提醒」中 `start_date` 不同的多条记录测试数据要求。
- 风险表第 13 条「M1 阶段提前事实化 Unknown 会污染 Engineering Requirement Card」的元约束。
- 第 12 节「代码调查中已确认的是『当前如何实现』，不是『本次必须如何实现』」的引导语。

---

## 3. Candidate B: Claude 评价

### 优点
- 领导原话与补充线索完整保留。
- **第 6 节「期望结果」明确分离「展示项目层面（已确认）」与「最新判定口径层面（必须保留为 Unknown）」**，是三张 Candidate 中边界最清晰的处理，最直接对应「最新口径不得提前事实化」这一关键风险。
- AC 段专门列出「依赖 Unknown 的待补 AC」（集計 / CSV / API / 权限可见性 / 多语言），结构最清晰，frozen 后扩展路径明确。
- Unknowns 14 条，风险 13 条，覆盖面与 Codex 相当。
- 集計レポート与表形式レポート的承载机制差异被显式标注。
- 权限可见性作为 Unknown #14 保留，并在第 11 节风险 #12 与「本次不主动改权限控制」的推测之间显式标出「解释空间」。
- 完全没有混入实现方案。

### 问题
- 与 Codex 类似，第 5 节也引用了「`customer_notes.max(id)` 链路」这一现有实现表述（在第 6 节 Unknowns 中），属于代码调查结论的轻度引入；但被严格限定为「现有口径」而非「本次口径」，问题较轻。
- 第 11 节风险 #9 表述「视觉最新 ≠ 严格最新」相对抽象，未明确指出基准差异的具体形式（这点 DeepSeek 反而更具体，但 DeepSeek 也因此泄露了实现细节，属于权衡）。

### 可吸收内容
- 第 6 节「展示项目」与「最新判定口径」的显式二分结构 —— 这是最值得吸收到主卡的设计。
- 第 10 节「依赖 Unknown 的待补 AC」分段写法。
- 第 11 节风险 #9「视觉最新 ≠ 严格最新」的隐性偏差概念。

---

## 4. Candidate C: DeepSeek 评价

### 优点
- 领导原话与补充线索完整保留。
- 第 6 节末尾显式注明「本节将『表示する項目』と『値を決定するロジック』を分離して記述」，与 Claude 的二分思想一致。
- 第 11 节风险 #3 指出了 `/admin/cdr/index` 默认表示順 `id DESC` 与严格 `start_date DESC` 不一致的隐性偏差，比 Codex / Claude 更具体（但同时也是一个问题，见下）。
- 用日语 / 中日混排表述 Unknowns 与 AC，对原始业务方语境保真度较高。
- 集計レポート与表形式レポート的差异显式标注。

### 问题
- **轻度混入了代码调查的实现细节**：
  - 第 5 节「当前现状」中「当前 `顧客最新情報` 链路中的"最新信息"现有实现口径与 `customer_notes` 的最新记录（最大 `id`）有关」—— 比 Codex / Claude 的同类描述更具体，已经接近实现细节。
  - 第 5 节「`/admin/cdr/index` 中存在 `発着信時間`，其数据来源为 `cdr.start_date`」—— 「数据来源为 `cdr.start_date`」属于代码调查结论的显式引入。
  - 第 11 节风险 #3「`/admin/cdr/index` 的デフォルト表示順が `id DESC`」—— 进入了 ORDER BY 级别的实现细节。
- **Unknowns 数量偏少（11 条）**：缺少「多语言文案 / 翻译范围」「列宽 / 排序 / 筛选默认行为」「表形式 CSV / 集計 CSV 是否分别属于范围」等条目（CSV 仅笼统提及一次）。
- **风险数量偏少（10 条）**：缺少「代码调查不能替代业务确认」级别的元风险；缺少「列宽 / 排序 / 筛选」表现层风险。
- 第 12 节虽然提到「代码调查報告書」的引用约束，但比 Codex / Claude 略简短。
- 没有把「最新 = `Cdr.start_date` 最大值」当成事实，这一点合格；但风险 #3 的表述方式（直接引用 `id DESC` 排序基准）已经为下游工程卡引入了潜在的实现倾向。

### 可吸收内容
- 第 6 节关于「分離して記述」的元说明。
- 风险 #3 的核心思想（视觉最新 ≠ 严格最新），但表述应抽象化，去除 `id DESC` 这种实现细节。

---

## 5. 横向对比表

| 维度 | Codex | Claude | DeepSeek |
|---|---|---|---|
| 1. 保留领导原话 | ✓ 完整 | ✓ 完整 | ✓ 完整 |
| 2. 准确识别主目标 | ✓ | ✓ | ✓ |
| 3. 区分 已确认/推测/未知 | ✓ 严格 | ✓ 严格 | ✓ 严格 |
| 4. 表形式/集計范围差异 | ✓ 标注差异 | ✓ 标注差异 | ✓ 标注差异 |
| 5. 「最新」口径保留为 Unknown | ✓ Unknowns #3 | ✓ Unknowns #3，且第 6 节显式二分 | ✓ Unknowns #3，但「现状」段轻度具体化 |
| 6. 没有进入实现方案 | ✓ | ✓ | △ 现状/风险段轻度泄露实现细节（`max(id)` / `id DESC`） |
| 7. AC 可手动验证 | ✓ | ✓（含「待补 AC」段） | ✓ |
| 8. Unknowns 完整性 | 14 项，最全 | 14 项，最全 | 11 项，偏少 |
| 9. 风险与歧义充分性 | 13 项 | 13 项 | 10 项 |
| 10. 适合作 frozen 基础 | 高 | 最高 | 中（需先剔除实现细节） |
| CSV / API 范围未知 | ✓ 分项列出 | ✓ 分项列出 | △ 仅笼统提及 |
| 权限可见性 | ✓ Unknowns #12 | ✓ Unknowns #14（含风险 #12） | ✓ Unknowns #11 |
| 多语言 / 列宽 / 排序 | ✓ Unknowns #14 / 风险 #11 | ✓ Unknowns #12, #13 / 风险 #13 | ✗ 未涵盖 |

---

## 6. 关键风险保留情况

| 关键风险 | Codex | Claude | DeepSeek |
|---|---|---|---|
| 「最新 = `Cdr.start_date` 最大值」未被事实化 | ✓ | ✓ | ✓ |
| 集計レポート范围未确认风险 | ✓ 风险 #2 | ✓ 风险 #2 | ✓ 风险 #2 |
| 表形式 CSV / 集計 CSV 范围未知 | ✓ 分项 Unknowns | ✓ 分项 Unknowns | △ 仅合并提及 |
| `api_find_report` 范围未知 | ✓ Unknowns #7 | ✓ Unknowns #7 | ✓ Unknowns #7 |
| `顧客最新情報` 现行最新口径 ≠ 「発着信履歴最新」 | ✓ 风险 #1 | ✓ 风险 #1 | ✓ 风险 #1 |
| `/admin/cdr/index` 视觉最新 ≠ 严格最新 | ✓ 风险 #9（抽象） | ✓ 风险 #9（抽象） | ✓ 风险 #3（但引入了 `id DESC` 实现细节） |
| 元风险：Unknown 事实化污染 Engineering 卡 | ✓ 风险 #13 | ✓ 风险 #11 | ✓ 风险 #10 |
| 代码调查 ≠ 业务确认 | ✓ 风险 #12 | ✓ 风险 #10 | ✓ 风险 #7 |
| 角色级权限可见性 | ✓ | ✓（含与「不改权限」推测的解释空间） | ✓ |
| 多语言 / 表现层默认行为 | ✓ | ✓ | ✗ 未保留 |

---

## 7. 推荐主卡

**推荐 Candidate B: Claude 作为后续 frozen requirement card 的主卡基础。**

核心理由：

1. **第 6 节对「展示项目」与「最新判定口径」的显式二分**，直接、显眼地隔离了本任务最高优先级的口径风险，是三张卡中最难被下游工程卡误读的结构。
2. **第 10 节 AC 中显式分出「依赖 Unknown 的待补 AC」**，让 frozen 后的扩展路径（集計 / CSV / API / 权限 / 多语言）有清晰挂载点。
3. Unknowns 与风险的覆盖面与 Codex 持平，但行文比 Codex 紧凑、重复更少。
4. 没有像 DeepSeek 那样混入 `customer_notes.id 最大值` / `id DESC` 等代码调查实现细节。

Codex 是非常接近的候选，可作为副参考；DeepSeek 在被剔除实现细节、补足 Unknowns 后，部分条目可作为风险描述的二级来源。

---

## 8. 合并建议

以 **Claude (B)** 为主卡骨架，对 Codex (A) 与 DeepSeek (C) 中可吸收的内容做定向合并：

1. **保留 Claude 的整体结构与第 6 节二分**（展示项目 / 最新判定口径），不要改写为单一段落。
2. **从 Codex 吸收**：
   - 第 12 节末尾的「测试数据准备提醒」（同一顧客に `start_date` の異なる複数件の発着信履歴データを準備）。
   - 风险表关于「代码调查中已确认的是『当前如何实现』，不是『本次必须如何实现』」的引导语，作为元约束置于第 12 节。
   - Unknowns 中若 Claude 有遗漏的细分项（如「表形式 CSV / 集計 CSV 是否分别属于范围」）需对齐。
3. **从 DeepSeek 吸收（需改写）**：
   - 第 11 节风险 #3「`/admin/cdr/index` 视觉最新 ≠ 严格最新」的具体场景描述，**但必须删除 `id DESC` 这种 ORDER BY 级实现细节**，改为抽象的「该画面的默认排序基准与严格的『最新』口径可能不一致」（Claude 风险 #9 已是这种抽象形式，可直接保留 Claude 原文）。
4. **明确不吸收**：
   - DeepSeek 第 5 节「数据来源为 `cdr.start_date`」「`customer_notes` 的最新记录（最大 `id`）」等接近实现的现状描述 —— 保留 Claude 与 Codex 的抽象级表述即可。
5. **统一表述**：将合并卡中所有「最新」相关 AC 与 Unknown 全部加注「以 Unknowns #3 确认结果为准」尾注，确保 frozen 后下游工程卡无法在未确认前消解此项。
6. **保持 Unknowns 与风险的元约束置顶**：「以上 Unknowns 必须保留，禁止在本卡片中替领导脑补回答」这句必须随 Claude 主卡一起进入 frozen 版本。

---

## Candidate A: Codex

# Requirement Card Candidate

## 1. 原始需求保真

领导原话（保留原文）：

```text
"【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。"

ベルシステム２４
```

补充原话 / 线索（保留原文）：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

已确认：
- 目标页面是 `admin/customer_contact_history_reports/add`
- 需求围绕 `顧客情報レポート` / `顧客最新情報`
- 希望新增一个可选项目 `発着信日時（最新）`
- 现状下需要从 `顧客対応履歴` 全履历中人工确认最新发信日付

推测：
- 领导原话中的“発信日付”在实际页面/报表项上，可能落为 `発着信日時（最新）`
- 该项目与 `発着信履歴` / `cdr` 存在业务关联

未知：
- 领导原话中的“発信日付”是否与补充线索中的 `発着信日時（最新）` 完全同义
- 本次验收是否覆盖表形式、集計、CSV、API 等全部相关出口

---

## 2. 一句话需求摘要

在 `admin/customer_contact_history_reports/add` 中，为 `顧客最新情報` 追加可选项目 `発着信日時（最新）`，让报表可展示该项目；但该字段“最新”的判定口径及影响范围仍待确认。

---

## 3. 用户真实目标

已确认：
- 降低用户为了确认最新发信相关时间而手工翻查 `顧客対応履歴` 的操作成本
- 让 `顧客情報レポート` 中的 `顧客最新情報` 直接承载该信息

推测：
- 用户希望在报表配置阶段勾选该项后，生成结果时直接看到对应值
- 用户希望该字段的使用方式与既存报表字段机制保持一致

未知：
- 用户真实目标是否仅限于“看见这个字段”
- 用户是否同时期待该字段参与集計、CSV、API 或其他导出/对外输出

---

## 4. 业务对象与操作路径

业务对象：
- `顧客情報レポート`
- `顧客最新情報`
- `顧客対応履歴`
- `発着信履歴`
- `cdr`

操作路径：

已确认：
- 用户进入 `admin/customer_contact_history_reports/add`
- 在 `項目（*必須）` 区域选择输出项目
- 勾选后生成 / 查看报表

已确认的字段呈现要求：
- 追加项名称：`発着信日時（最新）`
- 需要出现在 `出力 項目名` 列
- 需要出现在 `表示名` 列
- `順番` 放在最后一位

推测：
- 该项目的勾选与保存行为与既存项目机制一致
- 勾选后报表输出会以现有列展示机制呈现

未知：
- 完整菜单导航路径未确认
- “最后一位”是系统字段最后，还是包含自定义字段后的全列表最后，未确认
- 是否同样作用于 `表形式レポート` 与 `集計レポート`，未确认

---

## 5. 当前现状

已确认：
- `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 由既存字段定义生成
- 当前 `顧客最新情報` 对应字段列表中没有 `発着信日時（最新）`
- 当前报表输出依赖已保存的字段配置进行展示
- 当前 `顧客最新情報` 链路中的“最新信息”现有实现口径与最新 note 有关
- `/admin/cdr/index` 中存在 `発着信時間`

推测：
- 现状下用户无法在 `顧客最新情報` 报表中直接看到所需的最新发着信相关时间
- 现有“顧客最新情報”的最新口径与用户期待的“発着信日時（最新）”口径可能不一致
- 若集計レポート也纳入范围，当前代码调查显示其数据链路与表形式不同，影响范围可能扩大

未知：
- 无 `発着信履歴` 数据时，现有同类字段的期望展示方式
- 当前集計报表是否具备承载该字段的业务含义
- `api_find_report` 当前是否被业务方实际作为验收对象使用

---

## 6. 期望结果

已确认：
- 在 `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 中能看到 `発着信日時（最新）`
- 用户可以勾选该项目
- 勾选后，报表中可展示该客户对应的 `発着信日時（最新）`

推测：
- 该字段应遵循现有字段配置、显示、导出机制
- 若纳入既存报表出口，展示名应为 `発着信日時（最新）`

未知：
- “最新”应按 `Cdr.start_date` 最大值，还是沿用 `顧客最新情報` 现有最新 note 链路
- 无数据时显示空字符串、`-`、`（空白）` 或其他值，未确认
- 是否要求 `集計レポート` 也支持该字段
- 是否要求 CSV / API 同步体现
- 是否存在特定角色下该新增项目不可见 / 不可选的业务要求

---

## 7. 明确范围 In Scope

已确认纳入：
- `admin/customer_contact_history_reports/add` 页面中的 `項目（*必須）`
- 新增可选项目 `発着信日時（最新）`
- 该项目在 `出力 項目名`、`表示名` 的可见性
- 勾选后在报表结果中显示对应值这一业务目标

按材料需保留为待确认范围：
- `表形式レポート` 是否属于本次范围：有较强线索，但仍需确认
- `集計レポート` 是否属于本次范围：有线索，但未确认
- CSV 是否属于本次验收范围：未确认
- `api_find_report` 是否需要同步影响：未确认
- 表形式 CSV / 集計 CSV 是否属于本次范围：未确认

---

## 8. 明确不做 Out of Scope

本卡不包含（元约束）：
- 实现方案
- 代码修改指令
- SQL
- patch / diff
- Engineering Requirement Card
- Plan
- 对 Unknowns 的主观消解
- 将“可能相关文件”升级为“必须修改文件”
- 未经确认的业务规则补全

当前不能确认纳入本次的内容：
- Excel / PDF 输出
- batch / cron
- 邮件配信
- 其他类似页面统一改造
- 推测：本次不主动改权限控制

---

## 9. 需要确认 Unknowns

1. 这次是否只要求 `表形式レポート` 支持 `発着信日時（最新）`
2. `集計レポート` 是否也必须支持 `発着信日時（最新）`
3. `発着信日時（最新）` 的“最新”口径到底是 `Cdr.start_date` 最大值，还是现有 `顧客最新情報` 的最新 note 链路
4. 无 `発着信履歴` 数据时应该显示什么（空字符串 / `-` / `（空白）` / `N/A` / 其他）
5. `順番` 的“最后一位”是指系统字段最后，还是包含自定义字段后的全列表最后
6. CSV 是否属于本次验收范围
7. `api_find_report` 是否需要同步影响
8. 表形式 CSV / 集計 CSV 是否属于本次范围
9. 领导原话中的“発信日付”与补充项名 `発着信日時（最新）` 是否需要完全一致，还是允许作为同一业务含义处理；最终显示名以哪个为准，需以业务方书面确认为准
10. `/admin/cdr/index` 仅作为参考页面，还是其字段口径应被视为本需求标准来源
11. 完整菜单导航路径（从入口到 `admin/customer_contact_history_reports/add`）尚未确认
12. 是否存在特定角色下该新增项目不可见 / 不可选的业务要求
13. 列宽 / 排序 / 筛选默认行为是否需要对该新增项目作出特殊要求
14. 多语言文案 / 翻译范围是否属于本次要求

> 以上 Unknowns 必须保留，禁止在本卡片中替领导脑补回答。

---

## 10. 验收标准 Acceptance Criteria

正向项：

- AC-001：手动访问 `admin/customer_contact_history_reports/add`，在 `項目（*必須）` 中可以看到 `発着信日時（最新）`
- AC-002：手动确认 `発着信日時（最新）` 在 `出力 項目名` 列显示为 `発着信日時（最新）`
- AC-003：手动确认 `発着信日時（最新）` 在 `表示名` 列显示为 `発着信日時（最新）`
- AC-004：手动确认 `発着信日時（最新）` 的 `順番` 显示为最后一位（具体口径以 Unknowns #5 确认结果为准）
- AC-005：手动确认 `発着信日時（最新）` 可被正常勾选
- AC-006：保存配置后重新进入编辑或查看页面，`発着信日時（最新）` 的勾选状态保持一致
- AC-007：勾选 `発着信日時（最新）` 后手动生成 / 查看报表，报表中出现 `発着信日時（最新）` 列
- AC-008：准备一个存在多条 `発着信履歴` 的客户，手动验证报表中展示的是该字段对应的“最新”值；“最新”口径以 Unknowns #3 确认结果为准
- AC-009：当客户无 `発着信履歴` 时，报表中按 Unknowns #4 确认结果的口径显示
- AC-010：若业务确认 `集計レポート` 属于本次范围，则需补充并通过对应验收；以 Unknowns #2 确认结果为准
- AC-011：若业务确认 CSV 属于本次范围，则需补充并通过对应验收；以 Unknowns #6 / #8 确认结果为准
- AC-012：若业务确认 `api_find_report` 属于本次范围，则需补充并通过对应验收；以 Unknowns #7 确认结果为准

回归项：

- AC-Regression-001：未勾选 `発着信日時（最新）` 时，既存报表字段显示与现状一致
- AC-Regression-002：既存 `項目（*必須）` 中原有项目的显示名、順番、勾选行为不因本需求发生非预期变化
- AC-Regression-003：未勾选 `発着信日時（最新）` 或其他场景下，既有与発着信时间相关的现有行为不受本次新增字段影响

> 若本次验收范围不包含 CSV / 集計 / API，则验收时应明确排除这些出口，不作为本次通过条件。

---

## 11. 风险与歧义

| # | 风险 / 歧义 | 影响 / 深刻度 |
|---|-------------|---------------|
| 1 | `顧客最新情報` 现有“最新”实现口径与用户补充的“発着信履歴に一番最新→ cdr参照”可能不一致 | 做出来的值可能与业务预期不一致，出货后产生手戻 / 高 |
| 2 | `表形式レポート` 与 `集計レポート` 的字段承载机制并不相同，若范围未先确认，容易产生需求外扩 | 范围、工作量与验收边界发生变化 / 高 |
| 3 | CSV、表形式 CSV、集計 CSV、`api_find_report` 是否随字段新增联动未先界定 | 验收口径会分叉，重要导出/对外通路可能漏掉 / 中 |
| 4 | 领导说的是“発信日付”，补充说的是 `発着信日時（最新）`，字段名与业务含义是否完全等价存在歧义 | 命名 / 显示文案不一致，引发后续返工 / 中 |
| 5 | “発信”与“発着信”是否同一含义、是否包含着信，存在表述歧义 | 业务含义边界不清，影响数据取得范围理解 / 中 |
| 6 | “日付”与“日時”表述不一致，颗粒度（日 vs 时刻）尚未明确 | 输出格式与展示精度可能与业务预期不符 / 中 |
| 7 | “順番最后一位”缺少明确参照系（系统字段末尾 vs 含自定义字段的整体末尾） | 实际显示位置与业务预期不一致 / 低 |
| 8 | 无 `発着信履歴` 数据时的空值展示规则未明确 | 多处出口表现不一致，验收时易扯皮 / 低 |
| 9 | `/admin/cdr/index` 当前可见的“视觉最新”与严格按 `start_date` 取最大值并不天然等价 | 若以该页面为基准，可能引入口径偏差 / 高 |
| 10 | 是否存在特定角色下该新增项目不可见 / 不可选的业务要求尚未确认 | 上线后可能出现“部分角色看不到字段”的验收分歧 / 中 |
| 11 | 多语言文案、列宽、排序、筛选默认行为若被业务方隐含期待，但未在本阶段确认 | 容易在验收末期临时扩项 / 中 |
| 12 | 代码调查可用于澄清现状，但不能替代业务确认 | 易把“当前如何实现”误读为“本次必须如何实现” / 中 |
| 13 | 若在 M1 阶段提前把某个 Unknown 当成既定规则，会直接污染后续 Engineering Requirement Card | 范围与口径错误向下游传播 / 高 |

---

## 12. 给后续 Engineering Requirement Card 的输入提醒

后续进入 Engineering Requirement Card 前，至少应先明确以下内容：
- 本次范围是否仅限 `表形式レポート`
- `集計レポート` 是否纳入
- CSV、表形式 CSV、集計 CSV 是否纳入
- `api_find_report` 是否纳入
- `発着信日時（最新）` 的“最新”业务口径
- 无 `発着信履歴` 时的显示规则
- `順番最后一位` 的判定基准
- “発信日付” / `発着信日時（最新）` 的最终显示名称
- 是否存在特定角色下该新增项目不可见 / 不可选的业务要求
- 多语言文案 / 翻译范围是否纳入
- 列宽 / 排序 / 筛选默认行为是否有额外业务要求

同时提醒：
- 代码调查中已确认的是“当前系统怎么做”，不是“本次必须怎么做”
- `/admin/cdr/index` 只能作为参考线索，不能在未确认前直接等同为最终业务规则
- 代码调查报告中的所有“可能相关文件”“最小修改候选范围”“调用链”均只能作为后续 Engineering Requirement Card 阶段的参考，不得在本阶段升级为“必须修改文件”或实现指令
- 后续工程卡需要继续严格区分：已确认 / 推测 / 未知
- 本卡片所有保留的 Unknowns 与风险，必须在 Engineering Requirement Card 中逐项给出处置方式（确认 / 推迟 / 排除范围）
- 测试数据准备提醒：最新発着信判定の検証用に、同一顧客に `start_date` の異なる複数的発着信相关记录测试数据需要事先准备

---

## Candidate B: Claude

# Requirement Card Candidate

## 1. 原始需求保真

领导原话（保留原文，不改写、不美化）：

```text
"【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。"

ベルシステム２４
```

补充原话 / 线索（保留原文）：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

已确认：
- 目标页面是 `admin/customer_contact_history_reports/add`
- 需求围绕 `顧客情報レポート` / `顧客最新情報`
- 希望新增一个可选项目 `発着信日時（最新）`
- 现状下需要从 `顧客対応履歴` 全履历中人工确认最新发信日付

推测：
- 领导原话中的「発信日付」与补充原话中的「発着信日時（最新）」可能指同一业务含义
- 该项目的数据来源可能与 `発着信履歴` / `cdr` 有关

未知：
- 「発信日付」与「発着信日時（最新）」是否完全同义，最终显示名以哪个为准
- 本次验收是否覆盖表形式、集計、CSV、API 等全部相关出口

---

## 2. 一句话需求摘要

在 `admin/customer_contact_history_reports/add` 中，为 `顧客最新情報` 追加可选项目 `発着信日時（最新）`，使报表可显示该客户的「発着信日時（最新）」这一项目，但「最新」的具体判定口径及影响出口范围仍有待确认。

---

## 3. 用户真实目标

已确认：
- 降低用户为查看最新发信相关时间而手工翻查 `顧客対応履歴` 的操作成本
- 让 `顧客情報レポート` 中的 `顧客最新情報` 直接承载该信息

推测：
- 用户希望在报表配置阶段勾选该项后，生成结果时直接看到对应值
- 用户希望该项目的勾选与显示行为与现有项目机制保持一致

未知：
- 用户真实目标是否仅限于「在报表上看见这个项目」
- 用户是否同时期待该项目参与集計、CSV、API 或其他导出/对外输出

---

## 4. 业务对象与操作路径

业务对象：
- `顧客情報レポート`
- `顧客最新情報`
- `顧客対応履歴`
- `発着信履歴`
- `cdr`

操作路径：

已确认：
- 用户进入 `admin/customer_contact_history_reports/add`
- 在 `項目（*必須）` 区域选择输出项目
- 勾选后生成 / 查看报表

已确认的项目呈现要求：
- 追加项名称：`発着信日時（最新）`
- 需要出现在 `出力 項目名` 列
- 需要出现在 `表示名` 列
- `順番` 放在最后一位

推测：
- 该项目的勾选与保存行为与既存项目机制一致
- 勾选后报表输出会以现有列展示机制呈现

未知：
- 完整菜单导航路径未确认
- 「最后一位」是系统项目最后，还是包含自定义项目后的全列表最后，未确认
- 是否同样作用于 `表形式レポート` 与 `集計レポート`，未确认

---

## 5. 当前现状

已确认：
- `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 由既存项目定义生成
- 当前 `顧客最新情報` 对应项目列表中没有 `発着信日時（最新）`
- 当前报表输出依赖已保存的项目配置进行展示
- `/admin/cdr/index` 中存在 `発着信時間` 项目，作为参考画面被业务方提及

推测：
- 现状下用户无法在 `顧客最新情報` 报表中直接看到所需的「最新」発着信日時
- `顧客最新情報` 现有「最新」链路的判定口径与「発着信履歴的最新」可能不一致

未知：
- 无 `発着信履歴` 数据时现有同类项目的期望展示方式
- 当前集計报表是否具备承载该项目的业务含义
- `/admin/cdr/index` 是仅作为参考画面，还是其字段口径应被视为本需求的标准来源
- `api_find_report` 当前是否被业务方实际作为验收对象使用

---

## 6. 期望结果

（以下严格区分「展示项目」与「最新判定口径」两件事，不在本节锁定后者。）

已确认（展示项目层面）：
- 在 `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 中能看到 `発着信日時（最新）` 这一项目
- 用户可以勾选该项目
- 勾选后，报表中可展示该客户对应的 `発着信日時（最新）` 项目

推测：
- 该项目应遵循现有项目配置、显示、保存机制
- 若纳入既存报表出口，展示名应为 `発着信日時（最新）`
- 本次不主动改权限控制

未知（「最新判定口径」层面，必须保留）：
- 「最新」的判定口径是 `cdr.start_date` 最大值，还是沿用 `顧客最新情報` 既存的 `customer_notes.max(id)` 链路
- 无 `発着信履歴` 数据时显示空字符串、`-`、`（空白）`、`N/A` 或其他值
- 是否要求 `集計レポート` 也支持该项目
- 是否要求 CSV / API 同步体现
- 是否存在特定角色下该新增项目不可见 / 不可选的业务要求

---

## 7. 明确范围 In Scope

已确认纳入：
- `admin/customer_contact_history_reports/add` 页面中的 `項目（*必須）`
- 新增可选项目 `発着信日時（最新）`
- 该项目在 `出力 項目名`、`表示名` 的可见性
- 勾选后在报表结果中显示对应值这一业务目标

按材料需保留为待确认范围（不在本卡片中作出范围决断）：
- `表形式レポート` 是否属于本次范围：有较强线索，但仍需确认
- `集計レポート` 是否属于本次范围：补充原话中被提及，但代码调查显示其数据链路与表形式不同，影响范围可能扩大，是否纳入仍需确认
- CSV 是否属于本次验收范围：未确认
- `api_find_report` 是否需要同步影响：未确认
- 表形式 CSV / 集計 CSV 是否属于本次范围：未确认

---

## 8. 明确不做 Out of Scope

本卡不包含（元约束）：
- 实现方案
- 代码修改指令
- SQL
- patch / diff
- Engineering Requirement Card
- 对 Unknowns 的主观消解
- 将「可能相关文件」升级为「必须修改文件」
- 未经确认的业务规则补全

当前不能确认纳入本次的内容（仅作显式排除，不对 Unknown 项做范围决断）：
- Excel / PDF 输出
- batch / cron
- 邮件配信
- 其他类似页面统一改造

权限相关：
- 推测：本次不主动改权限控制
- 是否存在特定角色下该新增项目不可见 / 不可选的业务要求，保留为 Unknown（见 §9）

---

## 9. 需要确认 Unknowns

1. 这次是否只要求 `表形式レポート` 支持 `発着信日時（最新）`
2. `集計レポート` 是否也必须支持 `発着信日時（最新）`
3. `発着信日時（最新）` 的「最新」判定口径到底是 `cdr.start_date` 最大值，还是沿用 `顧客最新情報` 既存的 `customer_notes.max(id)` 链路
4. 无 `発着信履歴` 数据时应该显示什么（空字符串 / `-` / `（空白）` / `N/A` / 其他）
5. `順番` 的「最后一位」是指系统项目最后，还是包含自定义项目后的全列表最后
6. CSV 是否属于本次验收范围
7. `api_find_report` 是否需要同步影响
8. 表形式 CSV / 集計 CSV 是否属于本次范围
9. 领导原话中的「発信日付」与补充项名 `発着信日時（最新）` 是否需要完全一致，还是允许作为同一业务含义处理；最终显示名以哪个为准，需以业务方书面确认为准
10. `/admin/cdr/index` 仅作为参考画面，还是其字段口径应被视为本需求标准来源
11. 完整菜单导航路径（从入口到 `admin/customer_contact_history_reports/add`）尚未确认
12. 多语言文案 / 翻译范围未确认（仅日语，还是包含其他语言）
13. 列宽 / 排序 / 筛选的默认行为未确认
14. 是否存在特定角色下该新增项目不可见 / 不可选的业务要求（权限可见性）

> 以上 Unknowns 必须保留，禁止在本卡片中替领导脑补回答。

---

## 10. 验收标准 Acceptance Criteria

正向项：

- AC-001：手动访问 `admin/customer_contact_history_reports/add`，在 `項目（*必須）` 中可以看到 `発着信日時（最新）`
- AC-002：手动确认 `発着信日時（最新）` 在 `出力 項目名` 列显示为 `発着信日時（最新）`（显示名以 Unknowns #9 确认结果为准）
- AC-003：手动确认 `発着信日時（最新）` 在 `表示名` 列显示为 `発着信日時（最新）`（显示名以 Unknowns #9 确认结果为准）
- AC-004：手动确认 `発着信日時（最新）` 的 `順番` 显示为最后一位（具体口径以 Unknowns #5 确认结果为准）
- AC-005：手动确认 `発着信日時（最新）` 可被正常勾选
- AC-006：保存配置后重新进入编辑或查看页面，`発着信日時（最新）` 的勾选状态保持一致
- AC-007：勾选 `発着信日時（最新）` 后手动生成 / 查看报表，报表中出现 `発着信日時（最新）` 列
- AC-008：准备一个存在多条発着信履歴的客户，手动验证报表中展示的值符合「最新」判定口径（「最新」口径以 Unknowns #3 确认结果为准）
- AC-009：当客户无発着信履歴时，报表中按 Unknowns #4 确认结果的口径显示

回归项：

- AC-Regression-001：未勾选 `発着信日時（最新）` 时，既存报表项目显示与现状一致
- AC-Regression-002：既存 `項目（*必須）` 中原有项目的显示名、順番、勾选行为不因本需求发生非预期变化
- AC-Regression-003：未勾选 `発着信日時（最新）` 或其他场景下，既有 `顧客対応履歴` 等出口中与发着信時間相关的展示行为不受本次新增项目影响

依赖 Unknown 的待补 AC（确认后补充 AC）：

- 涉及 `集計レポート` 的 AC：以 Unknowns #2 确认结果为准，确认后补充 AC
- 涉及 CSV（表形式 CSV / 集計 CSV）的 AC：以 Unknowns #6 / #8 确认结果为准，确认后补充 AC
- 涉及 `api_find_report` 的 AC：以 Unknowns #7 确认结果为准，确认后补充 AC
- 涉及权限可见性的 AC：以 Unknowns #14 确认结果为准，确认后补充 AC
- 涉及多语言文案的 AC：以 Unknowns #12 确认结果为准，确认后补充 AC

> 若本次验收范围不包含 CSV / 集計 / API，则验收时应明确排除这些出口，不作为本次通过条件。

---

## 11. 风险与歧义

| # | 风险 / 歧义 | 影响 / 深刻度 |
|---|-------------|---------------|
| 1 | `顧客最新情報` 的「最新」业务口径与补充原话「発着信履歴に一番最新」之间可能不一致 | 做出来的值可能与业务预期不一致，出货后产生手戻 / 高 |
| 2 | `表形式レポート` 与 `集計レポート` 的项目承载机制可能不相同；若集計也纳入范围，影响范围可能扩大 | 范围、工作量与验收边界发生变化 / 高 |
| 3 | CSV、表形式 CSV、集計 CSV、`api_find_report` 是否随项目新增联动未先界定 | 验收口径会分叉，重要导出/对外通路可能漏掉 / 中 |
| 4 | 领导说的是「発信日付」，补充说的是「発着信日時（最新）」，项目名与业务含义是否完全等价存在歧义 | 命名 / 显示文案不一致，引发后续返工 / 中 |
| 5 | 「発信」与「発着信」是否同一含义、是否包含「着信」，存在表述歧义 | 业务含义边界不清，影响数据取得范围理解 / 中 |
| 6 | 「日付」与「日時」表述不一致，颗粒度（日 vs 时刻）尚未明确 | 输出格式与展示精度可能与业务预期不符 / 中 |
| 7 | 「順番最后一位」缺少明确参照系（系统项目末尾 vs 含自定义项目的整体末尾） | 实际显示位置与业务预期不一致 / 低 |
| 8 | 无 `発着信履歴` 数据时的空值展示规则未明确 | 多处出口表现不一致，验收时易扯皮 / 低 |
| 9 | `/admin/cdr/index` 当前可见的「视觉最新」与严格按「最新発着信日時」判定并不天然等价；该页面的默认排序基准与严格的「最新」口径可能不一致 | 若以该页面为基准，可能引入「视觉最新 ≠ 严格最新」的隐性偏差 / 高 |
| 10 | 代码调查可用于澄清现状，但不能替代业务确认 | 易把「当前如何实现」误读为「本次必须如何实现」 / 中 |
| 11 | 若在 M1 阶段提前把某个 Unknown 当成既定规则，会直接污染后续 Engineering Requirement Card | 范围与口径错误向下游传播 / 高 |
| 12 | 权限可见性是否存在角色级要求未确认，与「本次不主动改权限控制」的推测之间存在解释空间 | 角色相关的可见性边界可能在验收阶段才暴露 / 中 |
| 13 | 多语言文案 / 列宽 / 排序 / 筛选等表现层默认行为未确认 | 表现层与业务预期不一致，引发后续微调 / 低 |

---

## 12. 给后续 Engineering Requirement Card 的输入提醒

后续进入 Engineering Requirement Card 前，至少应先明确以下内容：
- 本次范围是否仅限 `表形式レポート`
- `集計レポート` 是否纳入
- CSV、表形式 CSV、集計 CSV 是否纳入
- `api_find_report` 是否纳入
- `発着信日時（最新）` 的「最新」业务判定口径
- 无 `発着信履歴` 时的显示规则
- `順番最后一位` 的判定基准
- 「発信日付」 / 「発着信日時（最新）」的最终显示名称
- 多语言文案的覆盖范围
- 列宽 / 排序 / 筛选的默认行为
- 是否存在角色级的可见性 / 可选性要求

同时提醒：
- 代码调查中已确认的是「当前系统怎么做」，不是「本次必须怎么做」
- `/admin/cdr/index` 只能作为参考画面，不能在未确认前直接等同为最终业务规则
- 代码调查报告中的所有「可能相关文件」「最小修改候选范围」「调用链」均只能作为后续 Engineering Requirement Card 阶段的参考，不得在本阶段升级为「必须修改文件」或实现指令
- 后续工程卡需要继续严格区分：已确认 / 推测 / 未知
- 本卡片所有保留的 Unknowns 与风险，必须在 Engineering Requirement Card 中逐项给出处置方式（确认 / 推迟 / 排除范围）
- 测试数据准备提醒：最新発着信判定の検証用に、同一顧客に発着信履歴データを複数パターン（最新判定基準が分かれうるケースを含む）用意するテストデータが必要

---

## Candidate C: DeepSeek

# Requirement Card Candidate

## 1. 原始需求保真

领导原话（保留原文）：

```text
"【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。"

ベルシステム２４
```

补充原话 / 线索（保留原文）：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

**已确认**  
- 目标页面是 `admin/customer_contact_history_reports/add`  
- 需求围绕 `顧客情報レポート` / `顧客最新情報`  
- 希望新增一个可选项目 `発着信日時（最新）`  
- 现状下，用户需要从 `顧客対応履歴` 全履历中人工确认最新发信日付  

**推测**  
- 领导原话中的“発信日付”在实际页面/报表项上可能落为 `発着信日時（最新）`  
- 数据来源可能与 `cdr` 相关  

**未知**  
- 领导原话中的“発信日付”与补充线索中的“発着信日時（最新）”是否完全同义  
- 本次验收是否覆盖表形式、集計、CSV、API 等全部相关出口  

---

## 2. 一句话需求摘要

在 `admin/customer_contact_history_reports/add` 页面中，为 `顧客最新情報` 对应报表新增可选项目 `発着信日時（最新）`，以减少用户手工翻查履历的成本；但该项目所展示的“最新”判定方法以及具体影响范围（表形式 / 集計 / CSV / API 等）尚未确认。

---

## 3. 用户真实目标

**已确认**  
- 降低用户为查看最新发信相关时间而手工翻查 `顧客対応履歴` 的操作成本  
- 让 `顧客情報レポート` 中的 `顧客最新情報` 直接承载该信息  

**推测**  
- 用户希望在报表配置阶段勾选该项后，生成结果时直接看到对应值  
- 用户希望该字段的行为与现有报表字段机制保持一致  

**未知**  
- 用户真实目标是否仅限于“看见这个字段”，还是同时期待该字段参与集計、CSV、API 或其他导出 / 对外输出  
- 是否存在特定角色下该新增项目不可见或不可选的业务约束  

---

## 4. 业务对象与操作路径

**业务对象**  
- `顧客情報レポート`  
- `顧客最新情報`  
- `顧客対応履歴`  
- `発着信履歴`  
- `cdr`  

**操作路径**  

**已确认**  
- 用户进入 `admin/customer_contact_history_reports/add`  
- 在 `項目（*必須）` 区域选择输出项目  
- 新增项名称：`発着信日時（最新）`  
- 需要出现在 `出力 項目名` 列  
- 需要出现在 `表示名` 列  
- `順番` 放在最后一位  

**推测**  
- 该项目的勾选与保存行为与既存项目机制一致  
- 勾选后报表输出会以现有列展示机制呈现  

**未知**  
- 完整菜单导航路径未确认  
- “最后一位”是系统字段最后，还是包含自定义字段后的全列表最后，未确认  
- 是否同样作用于 `表形式レポート` 与 `集計レポート`，未确认  

---

## 5. 当前现状

**已确认**  
- `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 由既存字段定义生成  
- 当前 `顧客最新情報` 对应字段列表中没有 `発着信日時（最新）`  
- 当前报表输出依赖已保存的字段配置进行展示  
- 当前 `顧客最新情報` 链路中的“最新信息”现有实现口径与 `customer_notes` 的最新记录（最大 `id`）有关  
- `/admin/cdr/index` 中存在 `発着信時間`，其数据来源为 `cdr.start_date`  

**推测**  
- 现状下用户无法在 `顧客最新情報` 报表中直接看到所需的最新発着信日時  
- 现有“最新 note”口径与“最新 cdr”口径可能不一致  
- 集計レポートのデータ取得構造が表形式とは異なり、同フィールドを追加する場合には別途検討が必要になる可能性がある  

**未知**  
- 无 `発着信履歴` 数据时，现有同类字段的期望展示方式  
- 当前集計报表在业务上是否有承载该字段的合理性  
- `api_find_report` 当前是否被业务方实际作为验收对象使用  

---

## 6. 期望结果

**已确认**  
- 在 `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 中能看到 `発着信日時（最新）`  
- 用户可以勾选该项目  
- 勾选后，报表中可显示「発着信日時（最新）」列  

**推测**  
- 该字段应遵循现有字段配置、显示、导出机制  
- 若纳入既存报表出口，表示名应为 `発着信日時（最新）`  

**未知**  
- 该列中表示される「発着信日時（最新）」の値の“最新”判定ロジックは、**Cdr.start_date の最大値** なのか、既存の **customer_notes.id 最大値** による最新ノート経由なのか、あるいは他の方法なのか  
- 当該顧客に発着信履歴が存在しない場合の表示値（空文字、`-`、`（空白）`、`N/A` など）  
- 集計レポートでも同様のサポートが必要か  
- CSV 出力（表形式 / 集計）や API 応答にも波及させる必要があるか  

> 注：本節では“表示する項目”と“値を決定するロジック”を分離して記述しており、“最新＝Cdr.start_date 最大値”等を既定事実として扱っていない。

---

## 7. 明确范围 In Scope

**已确认纳入**  
- `admin/customer_contact_history_reports/add` ページの `項目（*必須）`  
- 新規選択項目 `発着信日時（最新）`  
- 当該項目の `出力 項目名`、`表示名` への表示  
- 当該項目をチェックした上でレポートを生成した際、レポート上に対応する値が表示されるというビジネス目標  

**按材料需保留为待确认范围**（ユーザー補足で言及があるが、最終範囲確定を要する）  
- `表形式レポート` が今回の対象範囲に含まれるか  
- `集計レポート` が対象範囲に含まれるか  
- CSV（表形式 CSV / 集計 CSV）が今回の受入範囲に含まれるか  
- `api_find_report` への同期影響が必要か  

---

## 8. 明确不做 Out of Scope

**本卡不包含（元约束）**  
- 実装方案  
- コード修正指示  
- SQL  
- patch / diff  
- Engineering Requirement Card  
- Unknowns への主観的解消  
- 「可能性のある関連ファイル」を「要修正ファイル」へ格上げすること  
- 未確認の業務ルール補完  

**当前不能确认纳入本次的内容**  
- Excel / PDF 出力  
- batch / cron  
- メール配信  
- 他画面への一律展開  
- 権限制御体系の全面的改定（例：新規ロール追加、既存ロール変更）  
- 本追加項目に対する特定ロールへの可視性制限の要否は未確認（次項 Unknowns 参照）  

---

## 9. 需要确认 Unknowns

1. 今回の要件が `表形式レポート` のみを対象とするのか  
2. `集計レポート` にも `発着信日時（最新）` のサポートが必要か  
3. `発着信日時（最新）` の“最新”判定ロジックは、`Cdr.start_date` 最大値か、既存 `顧客最新情報` の `customer_notes.id` 最大値ロジックか、その他か  
4. 発着信履歴が存在しない顧客の場合、当該項目はどのように表示すべきか（空文字 / `-` / `（空白）` / `N/A` など）  
5. `順番`の“最后一位”はシステムフィールド末尾を指すのか、カスタムフィールドを含む全リスト末尾を指すのか  
6. CSV（表形式 CSV / 集計 CSV）が今回の受入対象に含まれるか  
7. `api_find_report` への同期対応が必要か  
8. リーダーの原話「発信日付」と補足項目名「発着信日時（最新）」は完全に同一の意味か。最終的な表示名称をどちらに定めるか（業務側の書面確認が必要）  
9. `/admin/cdr/index` はあくまで参考画面か、それとも当該画面のフィールド定義を本件の標準仕様として扱うべきか  
10. 完全なメニュー遷移経路（入口から `admin/customer_contact_history_reports/add` まで）が未確認  
11. 本追加項目に対し、特定の権限ロールで不可視／選択不可とする業務要件が存在するか（現状、制限なしの前提でよいか）  

> 以上の Unknowns は必ず維持し、本カード内でリーダーに代わって回答を脳内補完してはならない。

---

## 10. 验收标准 Acceptance Criteria

**正向项**  
- AC-001：`admin/customer_contact_history_reports/add` に手動でアクセスし、`項目（*必須）` 内に `発着信日時（最新）` が表示されること  
- AC-002：`発着信日時（最新）` の `出力 項目名` 列の値が `発着信日時（最新）` であること  
- AC-003：`発着信日時（最新）` の `表示名` 列の値が `発着信日時（最新）` であること  
- AC-004：`発着信日時（最新）` の `順番` が最後尾に表示されること（具体的な基準は Unknowns #5 の確認結果に従う）  
- AC-005：`発着信日時（最新）` を手動でチェックできること  
- AC-006：設定保存後、再度編集／表示画面に入った際に `発着信日時（最新）` のチェック状態が維持されていること  
- AC-007：`発着信日時（最新）` をチェックした状態でレポートを生成／表示し、レポート上に `発着信日時（最新）` 列が存在すること  
- AC-008：複数の発着信履歴を持つ顧客を用意し、レポートに表示される当該顧客の `発着信日時（最新）` の値が、業務上取り決めた“最新”の日時と一致すること（“最新”の定義は Unknowns #3 の確認結果に従う）  
- AC-009：発着信履歴が存在しない顧客について、当該列の表示が Unknowns #4 の確認結果に従った表示であること  

**回归项**  
- AC-Regression-001：`発着信日時（最新）` をチェックしない場合、既存レポートの項目表示が従来通りであること  
- AC-Regression-002：`項目（*必須）` 内の既存項目の表示名、順番、チェック動作が本対応によって意図せず変化しないこと  
- AC-Regression-003：`発着信日時（最新）` をチェックしない状態、または `Cdr.start_date` が関係しない他のシナリオにおいて、既存の `Cdr.start_date` 関連挙動が影響を受けないこと  

**范围依赖 AC（确认后补充）**  
- 集計レポートに関する AC：Unknowns #2 の確認結果を待って補足する  
- CSV（表形式 / 集計）に関する AC：Unknowns #6 の確認結果を待って補足する  
- `api_find_report` に関する AC：Unknowns #7 の確認結果を待って補足する  

> 今回の受入範囲が集計／CSV／API を含まないと確定した場合、当該出口は合格判定から除外する。

---

## 11. 风险与歧义

| # | 风险 / 歧义 | 影响 / 深刻度 |
|---|-------------|---------------|
| 1 | `顧客最新情報` の現行“最新”実装（`customer_notes.id` 最大）と、ユーザーが想定する“cdr 参照”による最新発着信日時の定義が食い違う可能性 | 生成される値が業務期待と異なり、リリース後の手戻りを招く（高） |
| 2 | `表形式レポート` と `集計レポート` でフィールド追加の仕組みが異なり、範囲未確定のまま進行すると需要の拡大や仕様の不一致が生じる | 範囲・工数・受入基準が変動する（高） |
| 3 | `/admin/cdr/index` のデフォルト表示順が `id DESC` であり、厳密な `start_date DESC` とは一致しない。この画面の“見た目の最新”と最大値が同一でない場合、業務基準との乖離を生む | 暗黙の前提により誤った口径が設計に持ち込まれる（高） |
| 4 | CSV（表形式／集計）、`api_find_report` への波及が未確定 | 受入基準の分岐、重要な出力経路の見落とし（中） |
| 5 | リーダー発言「発信日付」と補足「発着信日時（最新）」の表記揺れ、および「発信」と「発着信」の意味範囲の曖昧さ | 項目名・表示ラベルの不一致、データ抽出範囲の解釈違い（中） |
| 6 | 「日付」と「日時」の粒度の違い（日単位か時刻単位か） | 出力フォーマット・表示精度が期待と異なる（中） |
| 7 | コード調査は「現在どう実装されているか」を示すに過ぎず、業務上の正解ではない | 調査結果をそのまま業務要件と誤認すると、要求と異なる設計になる（中） |
| 8 | 「順番 最后一位」の基準（システムフィールド末尾／全リスト末尾）が未確定 | 実際の表示位置が期待と異なる（低） |
| 9 | 無データ時の空値表示ルールが未確定 | 複数の出力先で表示が不統一になり、受入時に混乱を招く（低） |
| 10 | M1 段階で Unknown を事実化してしまうと、後続の Engineering Requirement Card に誤った前提が伝播する | 範囲・ロジックの誤りが下流工程に拡大する（高） |

---

## 12. 给后续 Engineering Requirement Card 的输入提醒

Engineering Requirement Card 着手前に、少なくとも以下を確定させること：  
- 本件の対象範囲が `表形式レポート` に限定されるか否か  
- `集計レポート` を対象に含めるか  
- CSV（表形式／集計）、`api_find_report` への波及要否  
- `発着信日時（最新）` の“最新”を決定する業務ロジック  
- 発着信履歴が存在しない場合の表示ルール  
- `順番` の“最后一位”の判定基準  
- 最終的な表示名称（「発信日付」か「発着信日時（最新）」か）  

また、以下の点を Engineering Requirement Card へ明示的に引き継ぐこと：  
- 本カードで保持しているすべての Unknowns およびリスクは、Engineering Requirement Card 内で「確認／推迟／範囲外」のいずれかに処置を明示すること  
- コード調査報告書の「関連可能性のあるファイル」「最小変更候補範囲」「呼び出しチェーン」は、あくまで後続工程での参考情報であり、M1 段階で「修正必須ファイル」や実装指示に格上げしてはならない  
- 現時点で確認できている現状の実装ロジック（`顧客最新情報` における最新 note 参照、`cdr` テーブルとの結合状況など）は「現在のシステムの振る舞い」であり、必ずしも今回の正解ではない  

**テストデータ準備に関する注意**  
- 最新発着信判定の検証のため、同一顧客に対して `start_date` の異なる複数件の `cdr` レコードを用意したテストデータが必要となる可能性が高い
