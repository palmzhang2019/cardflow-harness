# Model Input

- task_id: 99999-m1-auto-smoke-test
- stage: m1-requirement-card
- round: round-1
- task: decide-candidate

---

## Instruction

# M1 Requirement Card Decision Maker

你现在是 Requirement Card 决策 Agent。

你的任务：
基于 3 张 Requirement Card Candidate、07-comparison.md、08-score.json，生成 09-decision.md。

当前阶段：
只允许做 Requirement Card 抽卡决策。
不要生成新的 Requirement Card 正文。
不要进入 Engineering Requirement Card。
不要写代码、SQL、patch、diff、实现方案。

必须说明：
1. 选择哪一张 Candidate 作为主卡
2. 为什么选择它
3. 从其他 Candidate 吸收哪些内容
4. 丢弃哪些内容
5. 哪些 Unknowns 必须继续保留
6. 下一步合并时的具体规则

特别注意：
- 不能把 Unknowns 在决策阶段消除。
- 不能因为某个 Candidate 写得细，就吸收它的实现方案。
- DeepSeek 中出现的字段命名、SQL、性能设计、i18n key 等实现内容必须明确丢弃。
- 如果吸收 DeepSeek 内容，只能吸收需求层面的 Unknown / AC / 风险提示。
- 最终合并基准必须来自评分 winner，除非有强理由反转。

输出格式：

# Round 1 Decision

## 1. Decision Summary

## 2. Selected Base Candidate

## 3. Selection Reason

## 4. Content to Absorb

### 4.1 From Codex
### 4.2 From Claude
### 4.3 From DeepSeek

## 5. Content to Discard

## 6. Unknowns That Must Remain

## 7. Merge Rules for 10-selected-requirement-card.md

只输出决策报告正文。

---

## 07-comparison.md

# Round 1 Candidate Comparison

## 1. 总体结论

三张 Candidate 的需求保真度都尚可，原话均完整保留，主目标识别均正确，且都没有写出 SQL/patch/diff 形式的代码。但在"严格区分 已确认 / 推测 / 未知"、"是否把 Unknown 误当成已确认事实"、"是否混入隐性实现方案"这三个关键判定上，三者差距很大。

- **Codex**：唯一在每个章节（业务对象、现状、期望结果、范围）都内嵌"已确认 / 推测 / 未知"三层分类的卡片，对集計レポート / CSV / API / 最新口径全部明确保留为待确认。最适合作为 frozen requirement card 的基础。
- **Claude**：保真度高，AC 明确编号并含回归项，Unknowns 与风险章节非常严谨，明确警告"禁止脑补"。略弱于 Codex 之处在于章节级别没有把"推测"显式区分出来，但实质上没有把 Unknown 升级为事实。
- **DeepSeek**：在"未知 → In Scope/Out of Scope"上越界最严重，把"集計レポート不做"、"CSV 必须做"、"`Cdr.start_date` 最大值即为最新"等本应是 Unknown 的项目直接当成已确认事实写进了范围与逻辑章节，并混入了多处实现方案。需要大量返工才能用作 frozen 基础。

推荐主卡：**Codex**，吸收 Claude 的 AC 编号体系与回归项写法，对 DeepSeek 仅保留其风险表表达形式，**严禁吸收 DeepSeek 的 In Scope / Out of Scope 写法**。

---

## 2. Candidate A: Codex 评价

### 优点
- 严格的"已确认 / 推测 / 未知"三层分层贯穿"用户真实目标 / 业务对象 / 当前现状 / 期望结果 / 范围"等几乎每一节，是三张中分层最干净的一张。
- 在 §7 In Scope 把表形式/集計/CSV/api_find_report/表形式 CSV/集計 CSV 全部明确列入"需保留为待确认范围"，而不是擅自归入 In/Out of Scope。
- 在 §6 期望结果中对"最新"口径明确写明两条候选解释（`Cdr.start_date` 最大值 vs `customer_notes.id` 最大值），未做选择，未升级为事实。
- §12 明确写出"代码调查报告的'可能相关文件 / 最小修改候选范围'不能直接转写为'必须修改文件'"，对 Engineering Requirement Card 阶段的越界做了预防。
- 完全没有实现方案、SQL、表名修改、property file、num 分配等技术细节。
- AC 全部以手动操作描述，且 AC-8/9/10 明确写明"若本次验收范围包含 CSV / 集計"，避免提前锁死范围。

### 问题
- AC 数量较多但没有像 Claude 那样使用编号体系（AC-001 / AC-Regression-001），追溯时稍逊。
- §11 风险章节是分段叙述式，没有像 DeepSeek 那样的"风险表 + 深刻度"，可读性略弱。
- 部分表述较密集（如 §4 / §5 / §7 内的项目较多），快速浏览时不如 Claude 的项目式列举清晰。

### 可吸收内容
- §1～§7 的"已确认 / 推测 / 未知"三层分层结构 → 主卡核心结构。
- §7 把范围相关 Unknown 明确写为"待确认范围"而非直接归入 In/Out。
- §12 关于"代码调查报告只能作参考、不能升级为必须修改文件"的提醒。

---

## 3. Candidate B: Claude 评价

### 优点
- 原话保真完整，且明确标注引文区块。
- §9 Unknowns 列了 8 条，末尾明确写"以上 Unknowns 必须保留，禁止在本卡片中替领导脑补回答"，立场明确。
- §10 AC 使用 AC-001～AC-008 + AC-Regression-001～003 的编号体系，含回归测试条目，可追溯性最好。
- AC-008 / AC-009 明确写"最新口径以 Unknowns #3 确认结果为准"、"按 Unknowns #4 确认的口径显示"，把范围风险与 AC 显式绑定。
- §11 风险与歧义中专门指出 `/admin/cdr/index` 默认 `id DESC` 而非 `start_date DESC` 的参考页面口径风险，这一点比 Codex / DeepSeek 都更细。
- §12 明确指示"代码调查报告中的所有'可能相关文件''最小修改候选范围''调用链'均只能作为后续 Engineering Requirement Card 阶段的参考，不得在本阶段升级为'必须修改文件'或实现指令"。
- 完全没有混入实现方案。

### 问题
- 章节内部没有像 Codex 那样显式分"已确认 / 推测 / 未知"三层，§4 / §5 / §6 主要靠括号或独立的 Unknowns 章节兜底，分层颗粒比 Codex 粗。
- §6 期望结果直接写"勾选后，report 中能展示该客户「発着信履歴」中最新一条对应的 `発着信時間`"，未在该节同时显式提示"最新"口径仍待 §9 确认；虽然 AC 那一侧绑定了 Unknowns #3，期望结果一节里没绑定，孤立看会让读者误读为已确认结论。
- §7 In Scope 第 4 条"勾选该项目后，report 中能够展示该客户最新一条発着信履歴的 `発着信時間`"也存在与 §6 相同的措辞偏紧问题。

### 可吸收内容
- §10 AC 编号体系（AC-XXX + AC-Regression-XXX）。
- §11 关于 `/admin/cdr/index` `id DESC` 与 `start_date DESC` 不等价的歧义。
- §9 末尾"禁止脑补回答"的硬性约束写法。
- §12 关于"代码调查报告只能作后续阶段参考"的写法。

---

## 4. Candidate C: DeepSeek 评价

### 优点
- §11 用表格形式列出风险（含影响 / 深刻度），可读性较好。
- §9 Unknowns 中对"最新"两种候选口径（`Cdr.start_date MAX` vs `customer_notes.id MAX`）有清晰表述。
- §12 后续提醒数量最多，覆盖了最新判定、集計可选性、无数据表示、CSV/API、多语言、迁移、权限、测试数据等。

### 问题（关键违规点，必须指出）

1. **把 Unknown 写成了已确认事实（违反硬性要求）**：
   - §7 In Scope 直接写："**対象レポートタイプ: 表形式レポート（`type=1`）**"——这本应在 Unknowns 中保留，DeepSeek 在自己的 §9 里也确实把它当成 Unknown 列出，但 §7 又自相矛盾地写成了 In Scope 的确认事项。
   - §7 In Scope 写："顧客ごとに発着信履歴（cdr）の `start_date` が最大となるレコードを参照し、その `start_date` を表示する"——虽然括号加了"※口径は要確認、後述"，但**仍然把"最新 = `Cdr.start_date` 最大值"写进了 In Scope 的数据取得逻辑**，这正是任务说明里点名要警告的越界。
   - §7 In Scope 写："**レポート出力対応：表形式レポートの CSV ダウンロード**"——CSV 是否纳入本次范围在 §9 Unknown #5 中明确标为未知，但 §7 又写成了 In Scope。

2. **把 Unknown 写成 Out of Scope（同样越界）**：
   - §8 Out of Scope 写："**集計レポート（`type=2`）への対応（現時点では要件に含めない）**"——用户补充原话明确写了"* 表形式レポート / * 集計レポート"，是否纳入集計是 Unknown，不应在 M1 阶段直接划入 Out of Scope。
   - §8 Out of Scope 写："**API `api_find_report` の改修（必要かどうか未確認のため、意図的なスコープ外）**"——"未確認所以意図的スコープ外"在逻辑上是把 Unknown 当成 Out of Scope 处理，属于 M1 阶段不可接受的范围决断。

3. **混入实现方案（违反硬性要求）**：
   - §7 写："**システムフィールドとして `発着信日時（最新）` を定義**"——这是实现指令。
   - §7 写："**日本語ロケールのプロパティファイルに `発着信日時（最新）` を追加**"——具体到 i18n 文件级别的实现细节。
   - §12 写："**実装時に `num` の割り振り方を指示する**"——直接指向具体字段编号字段。
   - §12 写："**`ApplicationResources.properties` に加え、他言語（英語、中国語など）のリソースファイルにも同様のキーを定義**"——具体到文件名和键定义。
   - §12 写："**`privilege.conf` にレポートダウンロード用の未使用アクションが存在するため、今回の対応で混入させないよう注意する**"——这是代码调查结论被直接搬进需求卡。
   - §4 操作路径写到了"レポート編集: `admin/customer_contact_history_reports/edit`、レポート表示: 表形式レポート詳細画面"——这两个 URL/画面属于代码调查得到的结论，作为本阶段的"已确认操作路径"超出了领导原话给出的范围。

4. **"最新"口径风险处理矛盾**：
   §11 R1 已经清晰指出"最新口径不一致"是高风险，但 §7 In Scope 仍把 `start_date MAX` 写进数据取得逻辑，自相矛盾。这就是任务说明里要求必须指出的"把'最新 = Cdr.start_date 最大值'直接当成确定事实"的典型情形（虽然加了"※要確認"的括注，但写进 In Scope 数据逻辑本身就是越界）。

5. **AC-8 写法依赖于自己越界的 In Scope**：
   AC-8 "CSV ダウンロード（スコープに含む場合）"——CSV 是否纳入本身就是 Unknown，AC 不该单独成项，应像 Claude 那样把它与 Unknowns 显式绑定。

### 可吸收内容
- §11 的"风险表 + 影响 + 深刻度"表格形式可作为风险章节的展示模板。
- §12 关于"测试数据准备：同一顾客多条 cdr 不同 start_date"作为后续阶段的提醒，可吸收一句。
- §9 Unknown #2 中对"最新"两种候选口径的措辞较直白，可作为表达参考。

**不可吸收**：§7 In Scope、§8 Out of Scope 的写法，§4 操作路径中的画面级断言，以及 §12 中所有指向具体文件/字段/属性的实现细节。

---

## 5. 横向对比表

| 维度 | Codex | Claude | DeepSeek |
|---|---|---|---|
| 1. 是否保留领导原话 | ✓ 完整保留含引文区块 | ✓ 完整保留含引文区块 | ✓ 完整保留含引文区块 |
| 2. 是否准确识别主目标 | ✓ | ✓ | ✓ |
| 3. 是否区分 已确认 / 推测 / 未知 | ✓ 章节内显式三层分层 | △ 主要靠 Unknowns 章节兜底，节内未显式三层 | ✗ 多处把 Unknown 写成已确认 In Scope 或 Out of Scope |
| 4. 表形式/集計范围差异处理 | ✓ 保留为待确认范围 | ✓ Unknowns #1/#2 显式保留 | ✗ §8 把集計直接划入 Out of Scope |
| 5. "最新"口径风险保留 | ✓ §6 / §9 / §11 三处均保留为未确认 | ✓ Unknowns #3 + §11 风险均保留 | △ §11 指出但 §7 In Scope 把 `start_date MAX` 写进数据逻辑，矛盾 |
| 6. 是否没有进入实现方案 | ✓ 完全没有 | ✓ 完全没有 | ✗ 混入 i18n property / privilege.conf / num 分配 / 系统字段定义等 |
| 7. AC 是否可手动验证 | ✓ 全部可手动 | ✓ 全部可手动，且编号体系最规整 | ✓ 基本可手动 |
| 8. Unknowns 是否完整 | ✓ 10 条，覆盖最广 | ✓ 8 条且明确"禁止脑补" | △ 7 条，部分本应 Unknown 的项被自己写进了 In/Out of Scope |
| 9. 风险与歧义是否充分 | ✓ 高风险/歧义/过程风险三层 | ✓ 含 `id DESC` vs `start_date DESC` 等独有细节 | ✓ 表格形式直观，但与 §7 矛盾 |
| 10. 是否适合作为后续 frozen 基础 | ✓ 最适合 | ○ 适合，需在 §6/§7 削紧"最新"措辞 | ✗ 需大量返工：清理实现方案、撤回 In/Out of Scope 中的越界判定 |

---

## 6. 关键风险保留情况

| 关键风险 | Codex | Claude | DeepSeek |
|---|---|---|---|
| "最新 = `Cdr.start_date` 最大值" 是否被当成确定事实 | 否（明确列出两种候选，未选择） | 否（Unknown #3 显式保留） | **是**——§7 In Scope 数据逻辑中直接采用 `start_date` MAX 写法，仅加括注"要確認"，违反硬性要求 |
| 集計レポート范围是否被忽略或预设 | 否（保留为待确认范围） | 否（Unknown #2 显式保留） | **是**——§8 直接划入 Out of Scope，与用户补充原话中"* 集計レポート"冲突 |
| CSV / 表形式 CSV / 集計 CSV / API 范围是否保留为未知 | 否，三者均明确"未确认" | 否，Unknowns #6 / #7 明确保留 | **是**——§7 把 CSV 写进 In Scope；§8 把 API 写进 Out of Scope（理由是"未確認"） |
| 是否混入实现方案 | 否 | 否 | **是**——property file、num 分配、system field 定义、privilege.conf 等 |
| 是否把 Unknown 升级为已确认事实 | 否 | 否 | **是**——表形式 type=1、CSV、集計剔除、最新口径等多处 |

---

## 7. 推荐主卡

**推荐采用 Candidate A: Codex 作为主卡。**

理由：
1. 唯一一张在章节级别（而非仅在 Unknowns 章节）严格执行"已确认 / 推测 / 未知"三层分类的卡片，与 M1 阶段"严禁脑补、严禁把 Unknown 升级为事实"的原则最契合。
2. 对集計レポート / 表形式 CSV / 集計 CSV / api_find_report / "最新"口径五项关键 Unknown，均未在 In Scope / Out of Scope 中做任何范围决断，全部保留为"待确认范围"。
3. §12 明确警告"代码调查中的'可能相关文件 / 最小修改候选范围'不能直接转写为'必须修改文件'"，对后续 Engineering Requirement Card 阶段的越界做了预防性约束。
4. 完全没有实现方案 / SQL / property file / 字段编号 / 权限文件级别的细节。

Claude 作为次优，可在 Codex 基础上吸收其 AC 编号体系、回归项写法、`id DESC` vs `start_date DESC` 的细节歧义、"禁止脑补回答"的硬性声明。

DeepSeek 不可作为主卡基础，需先剥离越界部分后才能局部吸收。

---

## 8. 合并建议

以 Codex 为骨架，按以下方式合并：

1. **保留 Codex §1～§9 的整体结构与三层分层**（已确认 / 推测 / 未知）。

2. **AC 章节改用 Claude §10 的编号体系**：
   - AC-001、AC-002…的正向项
   - AC-Regression-001、AC-Regression-002…的回归项
   - 每个含范围依赖的 AC（如 CSV / 集計 / 最新口径）显式绑定 Unknown 编号，写为"以 Unknowns #X 确认结果为准"。

3. **§11 风险章节**：
   - 主体保留 Codex 现有的"高风险 / 歧义 / 过程风险"分层叙述。
   - 增加 Claude §11 中"`/admin/cdr/index` 默认 `id DESC` 并非 `start_date DESC`，'视觉最新'与严格最大值不天然等价"一条。
   - 可参考 DeepSeek §11 的表格视觉形式（但只采用表格形态，不采用其内容矛盾的措辞）。

4. **§9 Unknowns 末尾**追加 Claude 的硬性声明："以上 Unknowns 必须保留，禁止在本卡片中替领导脑补回答。"

5. **§12 后续提醒**：
   - 保留 Codex 现有内容（含"代码调查报告只能作参考"的核心警告）。
   - 追加 Claude §12 中"本卡片所有保留的 Unknowns 与风险，必须在 Engineering Requirement Card 中逐项给出处置方式（确认 / 推迟 / 排除范围）"。
   - 可从 DeepSeek §12 中**仅**吸收一句："最新発着信判定の検証用に、同一顧客に `start_date` の異なる複数の cdr を用意するテストデータが必要"，作为后续阶段的提醒，但**不要**搬入 DeepSeek 关于 property file、`num` 分配、`privilege.conf`、`ApplicationResources.properties` 等任何实现层级的提醒。

6. **明确不吸收的内容**：
   - DeepSeek §7 In Scope 中的所有具体实现指令（system field 定义、property file 添加、CSV ダウンロード対応等）。
   - DeepSeek §8 Out of Scope 中对集計レポート、`api_find_report` 的范围决断。
   - DeepSeek §4 中"レポート編集: `admin/customer_contact_history_reports/edit`、レポート表示: 表形式レポート詳細画面"等代码调查层级的"已确认操作路径"扩展。
   - DeepSeek §12 中所有指向具体文件名 / 字段名 / 权限文件的提醒。

合并后产出的 frozen requirement card 应仍保持：**在所有"最新"口径、表形式/集計/CSV/API 范围、順番"最后一位"判定基准、无数据显示规则、命名一致性**等关键点上不替业务方下结论，仅保留 Unknowns 待后续确认。

---

## 08-score.json

{
  "round": "round-1",
  "scores": {
    "codex": {
      "fidelity_to_original_request": 10,
      "requirement_clarity": 10,
      "scope_control": 10,
      "unknowns_quality": 10,
      "risk_detection": 10,
      "acceptance_criteria_quality": 9,
      "no_implementation_leakage": 10,
      "usefulness_for_next_stage": 10,
      "total": 79,
      "summary": "唯一在章节级别严格执行‘已确认/推测/未知’三层分类，关键Unknown均未在In/Out Scope中越界决断，完全没有实现方案泄漏，AC可手动验证但编号体系稍弱。最适合作为frozen基础。"
    },
    "claude": {
      "fidelity_to_original_request": 10,
      "requirement_clarity": 9,
      "scope_control": 8,
      "unknowns_quality": 9,
      "risk_detection": 10,
      "acceptance_criteria_quality": 10,
      "no_implementation_leakage": 10,
      "usefulness_for_next_stage": 9,
      "total": 75,
      "summary": "原话保真，AC编号与回归项最规整，风险识别细腻；但章节内未显式三层分层，期望结果措辞偏紧，易被误读为已确认，需微调。整体适合作为辅助参考。"
    },
    "deepseek": {
      "fidelity_to_original_request": 8,
      "requirement_clarity": 7,
      "scope_control": 2,
      "unknowns_quality": 4,
      "risk_detection": 9,
      "acceptance_criteria_quality": 6,
      "no_implementation_leakage": 1,
      "usefulness_for_next_stage": 3,
      "total": 40,
      "summary": "严重越界，把集計、CSV、最新口径等Unknown写成In/Out Scope事实，混入大量实现方案，AC依赖自身越界的范围。风险表格形式可取其形，不可取其内容。"
    }
  },
  "winner": "codex",
  "merge_policy": {
    "base_candidate": "codex",
    "absorb_from_codex": [],
    "absorb_from_claude": [
      "AC编号体系与回归项写法（AC-001…, AC-Regression-001…）",
      "id DESC vs start_date DESC 歧义及参考画面风险点",
      "§9 末尾‘禁止在本卡片中替领导脑补回答’硬性声明",
      "§12 后续提醒中‘所有Unknowns与风险必须在Engineering Requirement Card中逐项给出处置方式’"
    ],
    "absorb_from_deepseek": [
      "风险表格展示形式（仅表格形态，不采用其内容矛盾措辞）",
      "测试数据提醒：同一顾客多条cdr不同start_date的验证用测试数据"
    ],
    "discard": [
      "deepseek_in_scope_all_scope_decisions",
      "deepseek_out_of_scope_unconfirmed_items",
      "deepseek_implementation_details",
      "deepseek_code_survey_expanded_operation_paths",
      "deepseek_privilege_conf_and_resource_files",
      "deepseek_num_assignment_and_system_field_definition"
    ]
  }
}

---

## Candidate A: Codex

# Requirement Card Candidate

## 1. 原始需求保真

领导原话（保留原文）：

```text
"【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。"

ベルシステム２４
```

补充原话 / 线索（保留原文）：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

已确认：
- 目标页面是 `admin/customer_contact_history_reports/add`
- 需求围绕 `顧客情報レポート` / `顧客最新情報`
- 希望新增一个可选项目 `発着信日時（最新）`
- 现状下需要从 `顧客対応履歴` 全履历中人工确认最新发信日付

推测：
- “発信日付”在实际页面/报表项上可能落为 `発着信日時（最新）`
- 数据来源可能与 `cdr` 相关

未知：
- 领导原话中的“発信日付”是否与补充线索中的“発着信日時（最新）”完全同义
- 本次验收是否覆盖表形式、集計、CSV、API 等全部相关出口

## 2. 一句话需求摘要

在 `admin/customer_contact_history_reports/add` 中，为 `顧客最新情報` 追加可选项目 `発着信日時（最新）`，使报表可显示该客户“最新”的発着信日時，但“最新”口径及影响范围仍有待确认。

## 3. 用户真实目标

已确认：
- 降低用户为查看最新发信相关时间而手工翻查 `顧客対応履歴` 的操作成本
- 让 `顧客情報レポート` 中的 `顧客最新情報` 直接承载该信息

推测：
- 用户希望在报表配置阶段勾选该项后，生成结果时直接看到对应值
- 用户希望该字段的行为与现有报表字段机制保持一致

未知：
- 用户真实目标是否仅限于“看见这个字段”
- 用户是否同时期待该字段参与集計、CSV、API 或其他导出/对外输出

## 4. 业务对象与操作路径

业务对象：
- `顧客情報レポート`
- `顧客最新情報`
- `顧客対応履歴`
- `発着信履歴`
- `cdr`

操作路径：

已确认：
- 用户进入 `admin/customer_contact_history_reports/add`
- 在 `項目（*必須）` 区域选择输出项目
- 勾选后生成/查看报表

已确认的字段呈现要求：
- 追加项名称：`発着信日時（最新）`
- 需要出现在 `出力 項目名` 列
- 需要出现在 `表示名` 列
- `順番` 放在最后一位

未知：
- 完整菜单导航路径未确认
- “最后一位”是系统字段最后，还是包含自定义字段后的全列表最后，未确认
- 是否同样作用于 `表形式レポート` 与 `集計レポート`，未确认

## 5. 当前现状

已确认：
- `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 由既存字段定义生成
- 当前 `顧客最新情報` 对应字段列表中没有 `発着信日時（最新）`
- 当前报表输出依赖已保存的字段配置进行展示
- 当前 `顧客最新情報` 链路中的“最新信息”现有实现口径与 `customer_notes` 的最新记录有关
- `/admin/cdr/index` 中存在 `発着信時間`，其数据来源为 `cdr.start_date`

推测：
- 现状下用户无法在 `顧客最新情報` 报表中直接看到所需的最新発着信日時
- 现有“最新 note”口径与“最新 cdr”口径可能不一致

未知：
- 无 `発着信履歴` 数据时现有同类字段的期望展示方式
- 当前集計报表是否具备承载该字段的业务含义
- `api_find_report` 当前是否被业务方实际作为验收对象使用

## 6. 期望结果

已确认：
- 在 `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 中能看到 `発着信日時（最新）`
- 用户可以勾选该项目
- 勾选后，报表中可展示该客户对应的 `発着信日時（最新）`

推测：
- 该字段应遵循现有字段配置、显示、导出机制
- 若纳入既存报表出口，展示名应为 `発着信日時（最新）`

未知：
- “最新”应按 `Cdr.start_date` 最大值，还是沿用 `顧客最新情報` 现有 `customer_notes.id` 最大值链路
- 无数据时显示空字符串、`-`、`（空白）` 或其他值，未确认
- 是否要求 `集計レポート` 也支持该字段
- 是否要求 CSV / API 同步体现

## 7. 明确范围 In Scope

已确认纳入：
- `admin/customer_contact_history_reports/add` 页面中的 `項目（*必須）`
- 新增可选项目 `発着信日時（最新）`
- 该项目在 `出力 項目名`、`表示名` 的可见性
- 勾选后在报表结果中显示对应值这一业务目标

按材料需保留为待确认范围：
- `表形式レポート` 是否属于本次范围：有较强线索，但仍需确认
- `集計レポート` 是否属于本次范围：有线索，但未确认
- CSV 是否属于本次验收范围：未确认
- `api_find_report` 是否需要同步影响：未确认
- 表形式 CSV / 集計 CSV 是否属于本次范围：未确认

## 8. 明确不做 Out of Scope

本卡不包含：
- 实现方案
- 代码修改指令
- SQL
- patch / diff
- Engineering Requirement Card
- 对 Unknowns 的主观消解
- 将“可能相关文件”升级为“必须修改文件”
- 未经确认的业务规则补全

当前不能确认纳入本次的内容：
- Excel / PDF 输出
- batch / cron
- 邮件配信
- 权限调整
- 其他类似页面统一改造

## 9. 需要确认 Unknowns

1. 这次是否只要求 `表形式レポート` 支持 `発着信日時（最新）`
2. `集計レポート` 是否也必须支持 `発着信日時（最新）`
3. `発着信日時（最新）` 的“最新”口径到底是 `Cdr.start_date` 最大值，还是现有 `顧客最新情報` 的 `customer_notes.id` 最大值逻辑
4. 无 `発着信履歴` 数据时应该显示什么
5. `順番` 的“最后一位”是指系统字段最后，还是包含自定义字段后的全列表最后
6. CSV 是否属于本次验收范围
7. `api_find_report` 是否需要同步影响
8. 表形式 CSV / 集計 CSV 是否属于本次范围
9. 领导原话中的“発信日付”与补充项名 `発着信日時（最新）` 是否需要完全一致，还是允许作为同一业务含义处理
10. `/admin/cdr/index` 仅作为参考页面，还是其字段口径应被视为本需求标准来源

## 10. 验收标准 Acceptance Criteria

1. 手动访问 `admin/customer_contact_history_reports/add`，在 `項目（*必須）` 中可以看到 `発着信日時（最新）`
2. 手动确认 `発着信日時（最新）` 在 `出力 項目名` 列与 `表示名` 列均有显示
3. 手动确认 `発着信日時（最新）` 可被正常勾选并随报表配置保存
4. 在已勾选该项的前提下，手动生成并查看对应报表时，可以看到 `発着信日時（最新）` 字段被输出
5. 准备一个存在多条発着信履歴的客户，手动验证报表中展示的是“已确认口径下”的最新一条记录对应时间
6. 未勾选 `発着信日時（最新）` 时，手动确认既有报表字段显示不受影响
7. 手动确认既存项目的显示名、顺番、勾选行为不因本需求发生非预期变化
8. 若本次验收范围包含 CSV，则需手动下载对应 CSV 并确认 `発着信日時（最新）` 的列头与内容符合页面配置
9. 若本次验收范围包含集計レポート，则需手动验证集計侧是否存在该项及其行为符合确认后的需求口径
10. 若本次验收范围不包含 CSV / 集計 / API，则验收时应明确排除这些出口，不作为本次通过条件

## 11. 风险与歧义

高风险项：
- `顧客最新情報` 现有“最新”实现口径与用户补充的“発着信履歴に一番最新→ cdr参照”可能不一致，容易导致做出来的值与业务预期不一致
- `表形式レポート` 与 `集計レポート` 的字段承载机制并不相同，若范围未先确认，容易产生需求外扩
- CSV、表形式 CSV、集計 CSV、`api_find_report` 是否随字段新增联动，若不先界定，验收口径会分叉

歧义项：
- 领导说的是“発信日付”，补充说的是“発着信日時（最新）”，字段名与业务含义是否完全等价存在歧义
- “順番最后一位”缺少明确参照系
- 无数据时的空值展示规则未明确
- `/admin/cdr/index` 当前可见的“最新感知”与严格按 `start_date` 取最大值并不天然等价

过程风险：
- 代码调查可用于澄清现状，但不能替代业务确认
- 若在 M1 阶段提前把某个 Unknown 当成既定规则，会直接污染后续 Engineering Requirement Card

## 12. 给后续 Engineering Requirement Card 的输入提醒

后续进入 Engineering Requirement Card 前，至少应先明确以下内容：
- 本次范围是否仅限 `表形式レポート`
- `集計レポート` 是否纳入
- CSV、表形式 CSV、集計 CSV 是否纳入
- `api_find_report` 是否纳入
- `発着信日時（最新）` 的“最新”业务口径
- 无 `発着信履歴` 时的显示规则
- `順番最后一位` 的判定基准

同时提醒：
- 代码调查中已确认的是“当前系统怎么做”，不是“本次必须怎么做”
- `/admin/cdr/index` 只能作为参考线索，不能在未确认前直接等同为最终业务规则
- 代码调查报告中的“最小修改候选范围”不能直接转写为“必须修改文件”
- 后续工程卡需要继续严格区分：已确认 / 推测 / 未知

---

## Candidate B: Claude

# Requirement Card Candidate

## 1. 原始需求保真

领导原话：

```text
【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。

ベルシステム２４
```

用户补充原话：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

---

## 2. 一句话需求摘要

在 `admin/customer_contact_history_reports/add` 页面的 `項目（*必須）` 中追加一个可选择项目 `発着信日時（最新）`，使得在「顧客最新情報」也能直接显示该客户最新的発着信日時，无需用户从「顧客対応履歴」全履历中手动抽取。

---

## 3. 用户真实目标

让用户在「顧客最新情報」相关 report 中，能够一眼看到该客户最新的発着信日時，省去当前从「顧客対応履歴」全履历中人工抽取「最新の発信日付」的工作量。

---

## 4. 业务对象与操作路径

- 主画面（已确认）：`admin/customer_contact_history_reports/add`
- 改动区块（已确认）：`項目（*必須）`
- `項目（*必須）` 当前的列（已确认）：
  - `出力 項目名`
  - `表示名`
  - `順番`
- 追加项目（已确认）：
  - `出力 項目名`：`発着信日時（最新）`
  - `表示名`：`発着信日時（最新）`
  - `順番`：放在最后一位（具体最后一位的口径见 Unknowns）
- 参考画面（已确认）：`/admin/cdr/index`
- 参考字段（已确认）：`発着信時間`
- 数据含义（用户原话）：取「発着信履歴」中最新一条的 `発着信時間`，参照 cdr
- 完整菜单路径：未知

---

## 5. 当前现状

- 页面 `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 当前不存在 `発着信日時（最新）`。
- 当 `target=2（顧客最新情報）` 时，系统字段列表中没有 `Cdr.start_date`；只有 `target=1（顧客対応履歴）` 才有。（来自代码调查报告，仅作需求澄清依据）
- 现状下，用户需要打开「顧客対応履歴」浏览全部履历，自行抽取最新的発信日付。
- `target=2` 当前用于关联「最新」的口径，是按 `customer_notes` 最大 `id` 关联最新 note，再 left join 到 cdr。这与「按 `Cdr.start_date` 最大值取最新」是否一致，未知。（来自代码调查报告，仅作需求澄清依据）

---

## 6. 期望结果

- 在 `admin/customer_contact_history_reports/add` 的 `項目（*必須）` 中出现 `発着信日時（最新）`。
- `発着信日時（最新）` 可以被勾选。
- `出力 項目名`、`表示名` 两列都展示为 `発着信日時（最新）`。
- `順番` 放在最后一位。
- 勾选后，report 中能展示该客户「発着信履歴」中最新一条对应的 `発着信時間`。
- 既存 report 中其他项目的显示与行为不受影响。

---

## 7. 明确范围 In Scope

- 在 `admin/customer_contact_history_reports/add` 页面的 `項目（*必須）` 中追加 `発着信日時（最新）` 这一可选项目。
- 该项目在 `出力 項目名` 列与 `表示名` 列均显示为 `発着信日時（最新）`。
- 该项目的 `順番` 放在最后一位。
- 勾选该项目后，report 中能够展示该客户最新一条発着信履歴的 `発着信時間`。

---

## 8. 明确不做 Out of Scope

- 不修改既存项目的显示名、顺番、勾选行为。
- 不调整 `項目（*必須）` 现有的三列结构（`出力 項目名` / `表示名` / `順番`）。
- 不新增搜索条件。
- 不新增权限控制。
- 不实施任何具体技术方案（本卡片不含实现细节）。

---

## 9. 需要确认 Unknowns

1. 本次是否只要求「表形式レポート（type=1）」支持 `発着信日時（最新）`？
2. 「集計レポート（type=2）」是否也必须支持 `発着信日時（最新）`？
3. `発着信日時（最新）` 的「最新」口径是按 `Cdr.start_date` 最大值，还是沿用现有「顧客最新情報」的 `customer_notes.id` 最大值逻辑？
4. 当该客户无発着信履歴数据时，`発着信日時（最新）` 在 report 中应显示为：空字符串 / `-` / `（空白）` / `N/A` / 其他？
5. `順番` 的「最后一位」是指：
   - (a) 只在系统字段范围内放最后？
   - (b) 还是包含自定义字段（`CustomerContactExt.field*`）后的整张列表的最后？
6. CSV 输出（表形式 CSV / 集計 CSV）是否属于本次验收范围？
7. `api_find_report` 等对外 API 是否需要同步支持 `発着信日時（最新）`？
8. 完整菜单路径（从入口到 `admin/customer_contact_history_reports/add` 的导航路径）是什么？

以上 Unknowns 必须保留，禁止在本卡片中替领导脑补回答。

---

## 10. 验收标准 Acceptance Criteria

可手动验证项：

- AC-001：访问 `admin/customer_contact_history_reports/add`，在 `項目（*必須）` 列表中可见 `発着信日時（最新）`。
- AC-002：`発着信日時（最新)` 的 `出力 項目名` 列显示为 `発着信日時（最新）`。
- AC-003：`発着信日時（最新)` 的 `表示名` 列显示为 `発着信日時（最新）`。
- AC-004：`発着信日時（最新)` 的 `順番` 显示为最后一位（具体口径以 Unknowns #5 确认结果为准）。
- AC-005：`発着信日時（最新)` 可以被勾选。
- AC-006：保存配置后，重新进入编辑或查看页面，`発着信日時（最新)` 的勾选状态保持一致。
- AC-007：勾选 `発着信日時（最新)` 后生成 / 查看 report，report 中出现 `発着信日時（最新)` 列。
- AC-008：当客户存在多条発着信履歴时，report 中显示的是最新一条記録対应的 `発着信時間`（最新口径以 Unknowns #3 确认结果为准）。
- AC-009：当客户无発着信履歴时，report 中按 Unknowns #4 确认的口径显示。
- AC-Regression-001：未勾选 `発着信日時（最新)` 时，既存 report 输出与现状一致。
- AC-Regression-002：既存 `項目（*必須）` 中原有项目的显示名、順番、勾选行为不受影响。
- AC-Regression-003：`target=1（顧客対応履歴）` 既有的 `Cdr.start_date` 行为不受影响。

> 本次集計レポート / CSV / API 等验收项是否纳入，依赖 Unknowns #1、#2、#6、#7 的确认结果。

---

## 11. 风险与歧义

- 业务口径风险：领导原话只说「発信日付」，用户补充说「発着信日時（最新）→ cdr参照」。「発信」与「発着信」是否同一含义、是否包含着信，存在歧义。
- 「最新」口径风险：现有 `target=2` 的「最新」是 `customer_notes.id` 最大；用户语义上的「最新」可能是 `Cdr.start_date` 最大；两种口径在数据上可能不一致。
- 集計レポート影响范围未明：若 Unknowns #2 答案为「需要」，则影响面会从表形式扩展到集計（涉及 X/Y/V 下拉与集計 SQL 口径），可能超出当前直觉范围。
- 「順番 最后一位」歧义：系统字段最后位 vs 包含自定义字段的整体最后位，是两种不同的需求。
- 派生输出影响未明：CSV、`api_find_report` 是否纳入本次需求，会显著影响验收边界。
- 空值显示不一致风险：既存代码中无值显示存在空串 / `N/A` / `(空白)` 等多种表现，需事先统一预期，否则验收时易产生扯皮。
- 参考页面口径风险：`/admin/cdr/index` 默认排序为 `id DESC`，并非严格 `start_date DESC`；若以该页面的「视觉最新」为基准，需明确是否可接受这种近似。

---

## 12. 给后续 Engineering Requirement Card 的输入提醒

- 必须先消除 Unknowns #1 ~ #7，再进入 Engineering Requirement Card。
- 「最新」的口径必须由业务方书面确认，不允许由实施方自行裁决。
- 「順番 最后一位」必须由业务方明确选 (a) 还是 (b)。
- 集計レポート、CSV、`api_find_report` 是否在范围内，必须先得到明确答复，否则范围会失控。
- 代码调查报告（报告-1）中的所有「可能相关文件」「最小修改候选范围」「调用链」均只能作为后续 Engineering Requirement Card 阶段的参考，不得在本阶段升级为「必须修改文件」或实现指令。
- 本卡片中所有保留的 Unknowns 与风险，必须在 Engineering Requirement Card 中逐项给出处置方式（确认 / 推迟 / 排除范围）。
- 不允许在 Engineering Requirement Card 阶段擅自扩大或缩小本卡片的 In Scope / Out of Scope。

---

## Candidate C: DeepSeek

# Requirement Card Candidate

## 1. 原始需求保真

```text
"【① 顧客情報レポートについて】
「顧客最新情報」においても発信日付を表示できるようしたい
※現状は「顧客対応履歴」から全履歴を確認いただき、最新の発信日付を抽出いただく必要がございます。"

ベルシステム２４
```

用户补充原话 / 线索：

```text
発着信日時（最新）→ 発着信履歴に一番最新→ cdr参照

* 表形式レポート
* 集計レポート
```

---

## 2. 一句话需求摘要

`admin/customer_contact_history_reports/add` ページの「項目（*必須）」に「発着信日時（最新）」という選択肢を追加し、レポート上で各顧客の最新発着信履歴の日時を表示できるようにする。

---

## 3. 用户真实目标

- レポートを作成・閲覧する担当者が、顧客ごとの最新の発着信日時を一目で確認できるようにする。
- これにより、現在「顧客対応履歴」画面から手作業で最新日時を探している手間を省く。

---

## 4. 业务对象与操作路径

- **対象レポート種別**: 顧客情報レポート（コード上では `target=2`）
- **対象画面**:
  - レポート新規作成: `admin/customer_contact_history_reports/add`
  - レポート編集: `admin/customer_contact_history_reports/edit`
  - レポート表示: 表形式レポート詳細画面
- **操作の流れ**:
  1. ユーザーがレポート作成画面を開く
  2. 「項目（*必須）」ブロックに表示される選択肢一覧から「発着信日時（最新）」を探す
  3. チェックボックスを ON にする
  4. 「順番」は一覧の最後尾に自動設定されていることを確認する
  5. レポートを保存し、表形式で表示する
  6. 表示されたレポートの列に「発着信日時（最新）」が含まれ、各顧客の最新発着信日時が表示される

---

## 5. 当前现状

| 項目 | 現状 |
|------|------|
| 項目の有無 | `target=2`（顧客最新情報）のシステムフィールド一覧に「発着信日時（最新）」に該当する項目は存在しない。 |
| 情報取得手段 | ユーザーは「顧客対応履歴」画面から全履歴を確認し、目視で最新の発信日付を抽出している。 |
| レポート出力 | 表形式レポートに発着信日時の列は出力されない。 |
| 集計レポート | 集計レポートの選択肢（X/Y/V）にも当該項目は存在しない。 |

---

## 6. 期望结果

- `admin/customer_contact_history_reports/add` 画面の「項目（*必須）」に、`出力 項目名` 列と `表示名` 列に「発着信日時（最新）」が表示される。
- `順番` は当該ブロック内の最後尾に位置する。
- ユーザーがこの項目をチェックしてレポートを保存できる。
- 保存後、表形式レポートの表示画面およびダウンロード（CSV）において、顧客ごとに「発着信日時（最新）」の値が表示される。
- 表示される値は、**当該顧客の発着信履歴の中で最も新しい「発着信時間」** であること。

---

## 7. 明确范围 In Scope

- **対象レポートタイプ**: 表形式レポート（`type=1`）
- **対象集計対象**: `target=2`（顧客最新情報）
- **追加項目**:
  - システムフィールドとして `発着信日時（最新）` を定義
  - 画面（add / edit）の「項目（*必須）」一覧に当該項目を追加
  - 順番はシステムフィールド一覧の最後尾とする
- **レポート出力対応**:
  - 表形式レポートの画面表示（DataTables）
  - 表形式レポートの CSV ダウンロード
- **データ取得ロジック**:
  - 顧客ごとに発着信履歴（cdr）の `start_date` が最大となるレコードを参照し、その `start_date` を表示する（※口径は要確認、後述）
- **表示文言**:
  - 日本語ロケールのプロパティファイルに `発着信日時（最新）` を追加
- **回帰影響確認**:
  - 既存の「項目（*必須）」の他項目が影響を受けないこと
  - 当該項目を未チェックの場合、既存レポート出力内容が変わらないこと

---

## 8. 明确不做 Out of Scope

- **集計レポート（`type=2`）への対応**（現時点では要件に含めない）
- **CDR 一覧画面（`/admin/cdr/index`）の改修**
- **Excel / PDF 出力の新規追加**（現状存在せず、本件では追加しない）
- **バッチ / cron / メール配信への組み込み**（該当機能なし）
- **権限制御の新設・変更**
- **API `api_find_report` の改修**（必要かどうか未確認のため、意図的なスコープ外）
- **他ターゲット（`target=1`）への展開**
- **カスタム項目（`CustomerContactExt.field*`）の自動追加**

---

## 9. 需要确认 Unknowns

以下の事項は、本 M1 段階では **未確定** であり、後続工程での回答が必要です。

1. **レポート種別の範囲**  
   - ユーザー補足に「集計レポート」とあるが、今回のスコープは **表形式レポートのみ** か、それとも **集計レポートでも選択可能にする** 必要があるか。  
   - コード上、集計レポートは「項目（*必須）」とは別の X/Y/V 選択方式であるため、集計を含める場合は対応方式が大きく異なる。

2. **「最新」の定義**  
   - `発着信日時（最新）` の“最新”は、**Cdr.start_date の最大値** と定義してよいか。  
   - または、現在 `target=2` で使われている「顧客最新情報」のロジック（`customer_notes` の最大 `id` で最新ノートを特定し、それに紐づく cdr を参照）に合わせる必要があるか。

3. **無データ時の表示**  
   - 顧客に発着信履歴が存在しない場合、または該当する cdr レコードが取得できない場合、**何を表示すべきか**（空文字、`-`、`（空白）`、`N/A` など）。

4. **順番の「最後位」の具体的意味**  
   - 「順番」を最後尾に置くとは、**システムフィールド一覧の末尾** を指すか。  
   - あるいは、**システムフィールド＋カスタムフィールド（`CustomerContactExt`）を含む全項目リストの末尾** を指すか。  
   - 現状、カスタム項目はシステム項目の後ろに追加されるため、どちらに置くかで画面表示順が変わる。

5. **CSV ダウンロードの扱い**  
   - 表形式レポートの CSV ダウンロードは、**本件のスコープに必ず含める** 必要があるか。  
   - それとも、画面表示のみを優先し、CSV は別タスクとするか。

6. **API `api_find_report` への影響**  
   - 外部連携などで使用される `api_find_report` エンドポイントにも、同様に `発着信日時（最新）` の項目を追加する必要があるか。  
   - 特に、`target=2` のレポートが API 経由で呼ばれるケースを想定する必要があるか。

7. **`顧客最新情報` における「発信日付」と「発着信日時（最新）」の名称統一**  
   - リーダー原話では「発信日付」、ユーザー補足は「発着信日時（最新）」。最終的にシステム上で表示する文言は **発着信日時（最新）** で確定か。それとも「発信日付」と表記するか。

---

## 10. 验收标准 Acceptance Criteria

### AC-1: 項目表示
- **環境**: 本番同等環境にデプロイ後
- **操作**: 管理画面から `admin/customer_contact_history_reports/add` を開き、`campaign` と `target`（`顧客最新情報` に相当するもの）を選択する
- **期待結果**: 「項目（*必須）」の一覧に **`発着信日時（最新）`** という行が表示され、`出力 項目名` 列と `表示名` 列に当該文言が確認できること

### AC-2: 順番
- **操作**: AC-1 と同じ画面で「項目（*必須）」一覧を確認する
- **期待結果**: `発着信日時（最新）` の `順番` が、当該ブロック内の **最後尾の番号** であること（システムフィールドの最終行として表示されること）

### AC-3: チェックと保存
- **操作**: `発着信日時（最新）` のチェックボックスを ON にし、その他必須項目を入力してレポートを保存する
- **期待結果**: エラーなく保存され、編集画面で再度開いた際にチェック状態が維持されていること

### AC-4: レポート出力（表形式・画面）
- **操作**: 保存したレポートの「表形式」表示画面を開く
- **期待結果**: 表示されるテーブルの列に `発着信日時（最新）` が存在し、各顧客の行に **最新の発着信日時**（例: `2026-05-13 10:30:00`）が表示されること

### AC-5: レポート出力（最新判定）
- **操作**: テスト用に、同一顧客に対して複数の発着信履歴（cdr）を異なる日時で用意し、レポートを表示する
- **期待結果**: レポート上の `発着信日時（最新）` 列には、**最も新しい `start_date` の値** が表示されること（※口径が確定した定義に従う）

### AC-6: 無データ時の表示
- **操作**: 発着信履歴を１件も持たない顧客を含む状態でレポートを表示する
- **期待結果**: 当該顧客の `発着信日時（最新）` 列には、**事前に合意された無データ表示**（空文字 または `-` など）が表示されること

### AC-7: 回帰確認（項目非選択時）
- **操作**: `発着信日時（最新）` を **チェックせずに** 保存したレポート、および既存のレポートを開く
- **期待結果**: 既存の項目（`顧客ID`、`顧客名` など）が従来通り表示され、新たな列は追加されず、並び順も変わらないこと

### AC-8: CSV ダウンロード（スコープに含む場合）
- **操作**: `発着信日時（最新）` を含むレポートの「CSVダウンロード」を実行する
- **期待結果**: CSV のヘッダに `発着信日時（最新）` が含まれ、データ行には画面表示と同じ値が出力されること

---

## 11. 风险与歧义

| # | 风险 / 歧义 | 影響 | 深刻度 |
|---|-------------|------|--------|
| R1 | **「最新」の口径不一致**：現状の `target=2` は `customer_notes.max(id)` で最新ノートを特定し、そこから cdr を参照する仕様。一方、直感的には cdr.start_date の最大値を期待する可能性が高い。 | ビジネス要件と実装がずれると、表示される日時が“最新発着信”でなく“最新ノートの発着信”になる。出荷後に手戻りが発生する。 | 高 |
| R2 | **集計レポートの範囲未確定**：ユーザー補足に「集計レポート」の言及があるが、技術的には全く別構造。スコープに含める場合、追加の設計・実装コストが大きい。 | 最初から集計まで求められると、見積もり・期間が大きく変わる。後から追加依頼が来る可能性もある。 | 中 |
| R3 | **無データ表示の不統一**：複数のパスで空文字／`（空白）`／`N/A` などが混在しており、今回の要件で統一した表示ルールが決まっていない。 | UI 上の一貫性が損なわれ、ユーザーが混乱する。 | 低 |
| R4 | **順番の解釈揺れ**：カスタム項目を含む全リストの最後に置くべきか、システム項目だけの最後に置くべきかで、実際の表示位置が変わる。 | リリース後に「思っていた最後と違う」という指摘を受ける可能性がある。 | 低 |
| R5 | **CSV / API 影響の漏れ**：画面表示のみを想定して進めると、ダウンロードや外部連携で項目が欠落し、業務フローが破綻する。 | 重要な導出機能が使えず、問い合わせや障害扱いになる。 | 中 |
| R6 | **既存レポートの互換性**：既存レポートの設定データに新しいフィールド定義が混ざることで、予期しない表示崩れやエラーが発生する可能性がある。 | 全レポートの動作確認が必要になる。 | 中 |

---

## 12. 给后续 Engineering Requirement Card 的输入提醒

1. **最新判定ロジックの確定**  
   必ず `Cdr.start_date MAX` で取得するのか、現行の `customer_notes.id MAX` 経由のロジックを踏襲するのかを決定すること。後者の場合、項目名を「最新ノート発着信日時」などに変更する必要性も検討する。

2. **集計レポートの要否**  
   ユーザー補足から漏れなく拾うため、ステークホルダーに **集計レポートでも選択可能にする必要があるか** を再確認する。必要と判断された場合は、Engineering Requirement Card を分割し、集計レポート用の追加タスクとして定義する。

3. **無データ時の統一表示**  
   システム全体の方針または本レポート固有のルールとして、値が取得できない場合の表示（例: `-`）を決定し、全出力経路（画面、CSV、将来的なAPI連携）に適用する。

4. **順番の配置ルール**  
   システムフィールドの末尾に置くのか、カスタムフィールドを含めた末尾に置くのかを決定し、実装時に `num` の割り振り方を指示する。

5. **CSV および API の対応範囲**  
   少なくとも表形式レポートの CSV ダウンロードは同一スコープに含めることを推奨する。`api_find_report` については、利用実態を確認のうえ、必要に応じて子タスクを切る。

6. **多言語リソースの準備**  
   日本語のプロパティ `ApplicationResources.properties` に加え、他言語（英語、中国語など）のリソースファイルにも同様のキーを定義する必要があるか否かを確認する。

7. **既存データのマイグレーション**  
   既存のレポート設定に新しいフィールドが自動的に現れないよう、追加は新規作成/編集時のみ影響する設計とする。必要に応じて、既存レポートの「項目」一覧に当該フィールドが追加されないことを保証する。

8. **権限制御**  
   `privilege.conf` にレポートダウンロード用の未使用アクションが存在するため、今回の対応で混入させないよう注意する。新たな権限制御は不要だが、念のため Engineering Requirement Card に「権限変更なし」と明記する。

9. **テストデータの準備**  
   最新発着信の判定を検証するため、同一顧客に `start_date` の異なる複数の cdr を持つテストデータが必要となることを Engineering Requirement Card に記載しておく。
